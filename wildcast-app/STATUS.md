# WildCast — Build Status

**Repo:** https://github.com/IntoTheWild-Dev/wild-cast  
**Live (Vercel):** auto-deploys on every push to `main`

---

## What's working right now

### AI Suggest v1.2 rebuild — pair/queue model (built 2026-09-25, branch `feat/ai-suggest-v1.2`, **not yet merged or live-verified**)
Full rebuild of AI Suggest to Mark's v1.2 spec (`wildcast-generate-with-ai-spec.md`, brief in `claudedocs/glm-ai-suggest-v1.2-rebuild-instructions.md`). Replaces the old single-line generator and the old two-tab sheet schema entirely. Local `vite build` + eslint pass (only pre-existing errors remain); logic verified with three offline test suites (live-sheet smoke test, mocked-Anthropic handler test, queue-flow test driving the real `usePairQueue` hook). **The 12 test briefs in spec §11 have NOT been run against the real API yet — no `WILDCAST_COPY` key locally. That's Mark's stated finish line for this phase and the gate before opening a PR.**
- **New sheet:** reads `wolt-copy-kb-clean.xlsx` (sheet `1FQ5R_go_zlA3RXddZ3wddEMbTVpbOcOY`), tabs Copy Library / Skeletons / Patterns / Verticals. The Copy Library's first header cell carries an instruction note ("One row = one line … ID") — normalized in `campaignSheet.js`. Rules/Layouts/Fineprint tabs are deliberately not read by code (Rules live verbatim in the system prompt; Layouts must never be sent as examples; Fineprint is never AI-generated).
- **`api/ai-suggest.js`:** new request contract (`{ field, lang, brief }`, no legacy shape — both callers updated). Builds the §4.5 brief JSON (kinds placeholder/user_draft/kept; locked = other field typed/kept; mode rewrite only for `user_draft`), scores 8–15 examples (tier weight +4 lockup split +3 merchant +2 mechanic +1 city +1 category; caps: ≤3/merchant, ≥4 patterns, ≤2 tier C), skips S02 + CTA skeletons (except support role), injects the Patterns table at `{{PATTERNS_TABLE}}` into the verbatim §6 system prompt (cache_control ephemeral), forces the `return_pairs` tool (§7.1), 20 s timeout + 1 retry. Runs the §9 checks C1–C12 in code (`api/_lib/checks.js`), one repair call if <2 pairs survive, 502 with "Could not write a line. Try again." when nothing passes. Partner history (§4.4) and logging (§10) are deferred — `partner_history` always `[]`.
- **Pair/queue client (`src/lib/usePairQueue.js`, engine in FieldEditor):** one click = one line applied to the clicked field; the matching partner line is stored, so clicking the other field shows it free (T12 sequence verified). Re-click walks the queue and offers the "Matching sub-headline →" link under the other field when it still holds the previous partner line. Credits drop **once per batch** (queue serves and Choose preset are free); background refill fires when 1 untouched pair is left (costs its own credit, gated); cap = 4 batches/hour **per design, surviving context resets**; any context change (partner/offer/note/lang/template/field edits) drops the queue. Locked batches when the other field holds typed or kept text; rewrite mode when the clicked field itself is a user draft. §8.3 hints (logo, too-little-context, note-vs-offer) render under the AI row.
- **UI changes:** `AISuggest.jsx` is now a one-line-per-click button (no dropdown list, no DE/EN toggle — AI writes in the design's language); optional "✎ Note" input feeds `user_note`; out-of-credits modal shared per panel. `Choose preset` (§5.1) reads the same sheet: tier A/B only, lockup halves (setup) or sublines (support) that fit `max_chars_min_pt`, vertical-gated.
- **Per-template §4.1 field settings** live on the Headline/Sub-headline zones in `templateZones.js` (Option A designer+guided, Option B headline-only — B has no editable sub-headline zone yet, noted in code, spec §13) plus a name-keyed map for **Option C** (Figma-imported, zones live in Blob so they can't carry static config — `aiFieldSettingsFor()` merges by template name, matched on /option c/i). **All char numbers are Mark's §11 ESTIMATES (Option C: sub 20/24, head 9/11; Option B: sub 16, head 10/12; Option A ships with C's numbers — same layout family) and min_pt values are ~60% guesses. Julia owes the measured box limits; updating them is a one-line change per number.**
- **Prompt Brief chat** (`PromptBriefChat.jsx`) moved to the same contract: each suggestion call returns pairs and the chat shows the asked field's line from each (no queue there; credits still not deducted in chat).
- **Review-round fixes (2026-09-25, second commit on this branch)** — 10 issues caught in code review, all fixed and regression-tested:
  1. `api/presets.js` served headline lockups to EVERY field's "Choose preset" (offer/tc/restaurant_name/cta included) — non-AI fields now get an empty list.
  2. Prompt Brief chat rendered the user's own "Suggest with AI" click as an AI bubble (`from: 'ai'` — a regression of the original `from: 'user'`).
  3. A FAILED background refill consumed a batch-budget slot (inconsistent with the manual path's record-after-success). Note: the unconfirmed background credit charge itself is spec §8.1 as written ("build the next batch in the background… credits go down by 1 when a batch is made") — kept, flagged for Julia/Mark if they'd rather have a confirm.
  4. asset-level `static_text`/`other_fields` took the headline zone's list and silently dropped the sub-headline zone's — now a union of both (matters the moment Option B's sub-headline zone ships with its own static text).
  5. The char counter/input maxLength used `max_chars` while the AI + canvas accept `max_chars_min_pt`-sized lines (§8.1 auto-fit) — counter now uses the min-pt capacity, so valid lines don't show false red. (C1 itself intentionally wraps at `max_chars_min_pt` per the spec's own C1 definition and the §12 "(min pt)" dry-run expectations.)
  6. Formal-address check was case-insensitive: missed line-start "Ihr" AND false-flagged informal lowercase "sie"/"ihr" — now matched case-sensitively on the original text (`Sie|Ihnen|Ihr(e|n|em|es)` anywhere).
  7. Option C's settings matched only the display label — now matched on the stable slotKey/id OR the label.
  8. Double-clicking AI Suggest in the same tick could build two batches (busyField is state, commits late) — synchronous in-flight ref guard added; every exit path clears it.
  9. Text restored from a saved design had no provenance and read as `kept` — unknown provenance now reads as `user_draft`, so rewrite mode works on reopened designs and we never fabricate AI provenance.
  10. C2's substring escape hatch let a fabricated bare digit ("5") pass inside an allowed compound ("2x15€") — the escape now requires a unit/symbol token (`€/%/x`).
- **Known gaps / decisions to flag to Julia:**
  - The 12-brief live run (spec §11) is unrun — needs the Vercel `WILDCAST_COPY` key; raw JSON goes to Mark.
  - ~~`caps: true` passed but the canvas didn't auto-uppercase~~ — **resolved 2026-09-25 (Julia's call, pre-preview):** zones whose §4.1 settings declare caps (headline + sub-headline on A/B/C) render uppercased on the canvas, so exports inherit it; the stored field value keeps normal case, exactly like the Copy Library ("Potsdams neues" stored → "POTSDAMS NEUES" printed). Option A/B via their zone settings, Option C via the name/id map in templateZones.js. Canvas-typed text round-trips already uppercase (idempotent transform).
  - City/district: no field exists in the app (brief sends empty) — spec §0.1/Phase-1 decision stands; adding a city input to the briefing form would improve local-flavor lines (flagged, not built).
  - Retail is thin in the sheet (8 rows) — retail presets/Presets lists are short by design until Kaya/Gio fill the library (spec §12 note).
  - Presets don't filter static-text-redundant lines (e.g. "In Minuten bei dir." on Option C, which prints "IN MINUTEN GELIEFERT") — spec §5.1 doesn't ask for it; trivial follow-up if it bothers.

### Prompt Brief — AI chat that briefs a template (built + shipped to `main` 2026-09-19, as an MVP — Julia's call)
Replaces the landing page's "Create from brief" form path. Landing tiles are now **Prompt Brief / Start from a template / Edit a previous design**. Flow: Prompt Brief → template picker popup → chat page → finished-design popup (**Edit design** / **Send for review**).
- **Questions are generated per template** from its zones (`src/lib/promptBriefFlow.js`): partner → objective → project name, then one question per zone in the editor's field order (Option A = 10 questions, Option B = logo/headline/photo/CTA). Business type/format are implied by the template, not asked. The old "About" question was removed (2026-09-19). Known zone ids get friendly wording; unknown ones get a generic question from the zone's label/hint, so new Figma imports work without code (untested with a real import).
- **Haiku (`claude-haiku-4-5-20251001`) via `api/prompt-brief-chat.js`**, same `WILDCAST_COPY` key as AI Suggest. The app owns the question list; Haiku reads typed answers (several at once, natural language for chip questions), answers side questions, and phrases each next question. The server validates everything it returns (real step ids, real chip options, text must be the partner's own words, char limits, skips only for the step just asked or one named) and picks the next step itself. Any failure/timeout falls back to scripted questions (this is also what local `vite dev` does — no `/api`). **Julia confirmed live Haiku phrasing on the Vercel preview 2026-09-19.**
- **Suggest / Improve with AI** on headline + sub-headline, reusing `/api/ai-suggest` (Wolt copy KB). **Credits are NOT deducted in the chat yet.**
- **Uploads** go through the editor's own pipeline (`uploadImageForZone`: transparent-PNG rule, saved to the partner's Assets) or **Choose from Assets** (in-chat popup, partner's assets first, same folder rules) and carry into the editor on Edit design. **Any image zone beyond logo/photo (sticker, QR, ...) and any extra text zone (e.g. a promo `code` box) is carried too (fixed 2026-09-21)** - before that only logo and photo made it out of the chat, so an uploaded sticker or QR code was collected and silently dropped.
- **Finished popup renders the real filled template** (off-screen `TemplateCanvas` + `getPng`, same as the old candidate picker), falling back to the template's stock thumb. With no logo given it uses the partner's Assets logo, matching what Edit design opens with.
- **Edit design** hands the answers into the normal editor via `handleSelectTemplateFromBrief(template, briefOverride)`. **Send for review is real (2026-09-19):** it saves the finished design to the Design library **without opening the editor** (reuses `saveCandidateForReview`; uploaded `blob:` images are converted to data URLs first, as `doSave()` does) and shows the shareable `/?review=<id>` link with a Copy button inside the popup. Same behaviour as the editor's Send for Review: it creates a link, it does **not** email anyone. Failure shows an inline message with a retry; going Back and pressing it again reuses the same link (no duplicate save). The editor's Export-behind-review gate is session-local by design, so reopening a saved design later requires sending it for review again to unlock Export.
- **Also fixed on this branch (separate commit `65e4815`, cherry-pickable): missing `%` on flyers.** `WOLTCondBlack.otf` and `WOLTRegular.otf` ship with a zero-height `%` glyph, so "30% SPAREN" rendered "30 SPAREN" — surfaced 2026-09-11 when CondBlack was registered. Patched with `unicode-range: U+0025` faces borrowing `%` from the neighbouring WOLT weight (`src/index.css`, plus `%` sample text on two `document.fonts.load` calls in `TemplateCanvas.jsx`). Replace with corrected font files from the supplier and delete the patch. Print PDFs not checked for this.
- **Testing status:** Julia tried it on the Vercel preview and confirmed live Haiku phrasing. Not yet exercised with messy free-text answers or a real Figma-imported template — treat those as unverified.
- **Open items:**
  - ~~Option C not showing in the picker on the preview~~ — **resolved:** Option C (Restaurant Flyer, imported from Figma, live) shows on production's Templates page as of 2026-09-21, and Option D exists as an archived draft. It comes from Vercel Blob via `/api/list-templates`, so a preview that can't read Blob still won't list it.
  - ~~Long headlines touching both edges of Option B's panel~~ — **fixed 2026-09-19 (Julia's call, 92%):** `TemplateCanvas.jsx` auto-shrink now also requires a single unrotated line to fit within 92% of its zone's width (`FIT_WIDTH_RATIO`), on first load and while typing. Measured on Option B "Wen Cheng. Jetzt.": 100% of the panel (touching both edges) → 93%. Applies to every template's fresh loads/edits; saved designs keep their saved font sizes; wrapped paragraphs (T&Cs) and rotated zones are exempt. If a template ever needs a different margin, change the constant or add a per-zone override.
  - **Security/cost:** partner-level API routes (`ai-suggest`, `prompt-brief-chat`) don't check who is calling, so anyone with the URL could spend Anthropic credit. Input/output are capped and Haiku is cheap, but add a rate limit or short-lived token before real partners use it.
  - Credits for chat turns and retiring the old `BriefingForm` code are undecided/undone. Send for review sends no email/notification — reviewers only get the link if the partner shares it (a notification step is a possible follow-up).

### Editor redesign — Guided/Advanced, accordion, autosave, Send for Review (built 2026-09-18, PR #13, from Annika's mockup)
This was never written up here, which is why autosave and the new footer looked undocumented. The normal (non-restricted) editor now works like this:
- **Guided / Advanced toggle** in the breadcrumb bar replaces the old fixed guided-vs-designer split. Guided hides font-size/position controls and locks the canvas to nudge-only; Advanced shows full controls and free dragging. It is a live in-session switch (`effectiveMode` in `App.jsx`, passed to `FieldEditor` and `TemplateCanvas`); a template's `-simple` and plain ids always share the identical zone layout, so toggling only changes what is shown/locked. Restricted review mode (opened from a brief) has no toggle.
- **Accordion field list + "X of Y ready" progress bar.** Only the active field is expanded (coral-bordered box, matching the coral highlight of its canvas zone); the rest collapse to a one-line summary with a tick once filled. Steps are numbered in the shared order from `lib/fieldOrder.js`, matching the numbered chips on the canvas. Clicking a zone on the canvas edits it in place, kept in sync with the panel.
- **One AI icon per field.** Suggest and Improve are merged: an empty field generates fresh copy from the brief, a field with text polishes/translates it. Next to it, **Choose preset** serves real past campaign copy from the copy database (`api/presets.js`) — no AI, no credit cost, vertical-gated.
- **Autosave replaces the Save button.** There is **no autosave setting or toggle** — it is always on for the normal editor: 2.5 seconds after the last edit (only when something actually changed), it saves quietly in the background. The only feedback is a small chip beside the breadcrumb reading **Saving… / Saved just now / Saved**, and it only appears once the design has been saved at least once. Restricted review mode is deliberately not autosaved: it keeps its manual "Save & pick another design" flow.
- **Send for Review is the one primary (red) button.** It creates a shareable review link (no email is sent). **Export PDF is locked behind it** ("Export PDF — unlocks once you send for review") for the current editing session; the unlock is session-local by design, so reopening a saved design later means sending it for review again to export.
- **Editor has its own URL** (`/content/<id>`, Anang, PR #11): refreshing the page reopens the same design (sessionStorage first, then `api/load-project.js?id=`); `vercel.json` rewrites only that path to `index.html`.
- **Other editor and landing-page changes from Anang (his PRs #9-#15):** AI credits badge shows its explanation on click instead of hover; an image file can be dragged from the desktop straight onto the canvas; the drag-to-reorder steps and the per-image size hint text were removed again as clutter; the unused DE/EN switcher was removed (fields stay German); the landing page got a static hero headline and its "Coming soon" roadmap cards were added and then removed.
- **Gotcha (fixed 2026-09-21): moving or renaming a design must also update the browser's cached copy.** Every save writes the full project to `sessionStorage` (`wildcast_project_<id>`) and reopening reads that copy first. Designs page **Move to folder** and rename only patched the server, so reopening a moved design reloaded the old folder and the next autosave wrote it back. `DesignsPage.jsx` now patches the cached copy too, and a rejected move rolls back with an alert.

### Briefing Form → Template Picker → Mode → Live Editor (reworked 2026-09-08) — new front page
Client-requested workflow reversal, same day: the 2026-08-03 pivot below (3-step wizard → AI generates candidate mockups to pick from) is replaced with a single-screen brief → template picker → "Choose your mode" → live editor flow, much closer to the original pre-pivot template-first experience. See the 2026-09-08 update entries below for the full reasoning and build log.
- **Brief is one screen now**, just enough to route to the right template: Partner name, Business type, "what's this about", Objective (with per-choice follow-up), Formats needed. No pagination, no AI-candidate-generation step.
- **Template picking is Step 1, above the form (2026-09-10)** — a partner picks Option A or B first (single-select popup, closes itself on click), *then* fills in the rest of the brief. Business type/Formats show pre-filled from the pick but stay editable. Submitting without a pick shows a "Please pick your template first" popup rather than a disabled button. Submit then jumps straight to the "Choose your mode" popup for the already-picked template — the old post-brief template-select screen (`BriefTemplatePicker`'s card grid) is skipped entirely for this path (still used by the separate "Need more layouts?" popup, unchanged).
- The editor opens **pre-filled** with what the brief mapped (partner/restaurant name; merchant's logo auto-pulled from the Library). Offer/CTA, headline, subline, sticker, food photo, T&Cs, QR are all filled live in the editor itself with AI Suggest available per field, instead of two more wizard steps. Food photo deliberately does NOT auto-pull from the Library (2026-09-09) — always starts blank so a partner explicitly picks the dish for each design; Offer/CTA also no longer pre-fill with the raw objective label (2026-09-09) — both start blank with a greyed example instead.
- **"Formats needed" checkboxes are gated by what's actually live** for the selected Business type — checked against the same template data the picker itself uses (factors in Figma imports too), so a partner can't check a format the tool can't produce yet; shows greyed-out "· Coming soon" instead, no manual update needed as more formats go live.
- **"Need more layouts?" popup** after Save/Export/Send for Review — if the brief checked other formats too, offers to jump straight to a picker scoped to one of them, reusing the same brief answers instead of re-filling the form.
- **Send for Review now confirms first** ("...skips exporting or reviewing it here first") — previously a one-way action explained only in small footer text.
- **Export PDF states its own cost** ("Uses 1 export credit — N remaining"), same pattern AI Suggest already used.
- The old AI-candidate-generation flow (`TemplateCandidatePicker.jsx`, `TemplatePreviewModal.jsx`, the 3-step wizard, the locked-down "restricted review" editor mode — all documented in the 2026-08-03 entries below) is **not deleted, just unwired** from this primary path, in case it's wanted again.
- **⚠️ Not yet manually tested by Julia as of 2026-09-08** — built and verified locally (mocked `/api/*` calls, since local `vite dev` has no serverless backend) but she hasn't clicked through it herself yet. Treat as "should work," not "confirmed."

### Template Picker (redesigned 2026-06-30)
- **Two-level browse:** homepage shows group cards (e.g. "Restaurant Flyer · 5 designs"), clicking "View all →" drills into that group's option cards (A–E)
- **Search bar** at top of homepage — filters by format/category in real time
- **Headline:** "Design. Export. Print."
- 30 template slots total: 6 groups × 5 options (Restaurant + Retail × Flyer + Poster + Wild Poster)
- 2 live templates (Restaurant Flyer · Option A and Option B — both guided and designer modes)
- Coming soon tiles clearly labelled, greyed out with dashed border
- Group cards show live thumbnail, "X available" coral badge, "5 designs" count badge
- Options view shows "← All templates" back button, live count subtitle
- Mode picker modal on click: 4 options (Guided/Designer × Text-only/Text+Image)
- Category filter (Restaurant / Retail) and Format filter (Flyer / Poster · soon / Wild Poster · soon)

### Canvas Editor
- Fabric.js canvas with background PNG loaded from Vercel Blob
- Zoom via +/− magnifying glass buttons (no scroll wheel)
- Editable text zones: Headline, Sub-headline, Offer, T&Cs (rotated −90°)
- Per-field font size (− / + pt) and alignment (L / C / R) controls
- Drag to reposition text zones, drag right edge to reflow text width
- Per-field reset position (↺) + Reset layout button
- Pink (#FF3182) handles, Figma-style centre guide (drag only, hidden on export)
- Image upload: logo + food photo zones (Flyer 2), URL guard prevents interference
- **Guided mode:** teal "🔒 Guided mode · canvas locked" badge visible above canvas
- Image scale ±10% control in guided mode (appears after upload, 20–300%)
- clipPath on all images prevents bleed outside zone bounds
- **Zone boundary guides:** white dashed outlines on all zones (text + image), visible in both designer and guided mode, always hidden on export

### Field Editor (right panel)
- Numbered step layout for BOTH modes (same clean UI)
- Designer Mode: steps include font-size + alignment + reset controls
- Non-Designer (Guided) Mode: font-size ±pt controls on Headline, Sub-headline, Offer (not T&Cs); canvas locked
- Image upload zones with drag/drop style UI + scale control in guided mode
- AI Suggest buttons (mock copy, DE + EN)
- Language toggle DE / EN
- ICC Profile selector (FOGRA39, GRACoL, SWOP, Japan Color)
- **Export PDF** — CMYK PDF/X-4 via `/api/export-cmyk` (see below)
- **Send for Review** button — saves project + generates shareable link
- **Save button** — saves/re-saves project to Vercel Blob + localStorage registry

### Designs Tab
- Header nav: Templates · Designs · Help
- **Global** — every activation key sees every saved design (not per-browser), server-listed via `GET /api/save-project`
- "Find a design" popup (Format + Merchant dropdowns) gates the list before it renders; results grouped by format
- Opening a design (card click or "Continue editing") asks **"Edit original" or "Duplicate & edit a copy"** first — duplicating clones the full state under a new id so the source design is never overwritten
- Hover × delete button — deletes from Vercel Blob (project JSON + comments)
- Empty state with clear onboarding copy
- Projects stored in Vercel Blob (JSON, private) — no client-side registry

### Send for Review
- "Send for Review" button saves the project then shows a modal with a copyable share link
- Share link format: `wild-cast-psi.vercel.app/?review=<projectId>`
- Reviewer opens link → sees read-only flyer preview + comment panel
- Reviewer leaves name + comment → stored in Vercel Blob at `comments/<id>.json`
- Editor re-opens project from Designs → amber feedback panel appears on LEFT side of canvas
- Comments show reviewer name + date/time stamp
- Works across all 4 templates

### Export PDF (CMYK / PDF/X-4)
- "Export PDF" button sends the 4× canvas PNG to `/api/export-cmyk`
- Server resizes to **1311×1819px** (A6 + 3mm bleed at 300 DPI)
- Converts sRGB → FOGRA39 CMYK via `sharp` `.withIccProfile()` (ICC-aware, single-step conversion)
- Embeds **FOGRA39 (ISOcoated_v2_eci.icc)** — European print standard (ISO 12647-2:2004)
- Builds a **PDF/X-4** compliant document from scratch:
  - MediaBox: 111×154mm (314.6×436.5 pt) — full with bleed
  - TrimBox: 105×148mm (297.6×419.5 pt) — finished A6
  - BleedBox = MediaBox
  - OutputIntent: `/GTS_PDFIX` referencing embedded FOGRA39 ICC stream
  - ICC stream: FlateDecode compressed (1.8 MB → ~1.3 MB in PDF)
  - XMP metadata: `pdfx:GTS_PDFXVersion = PDF/X-4`, `pdfx:GTS_PDFXConformance = PDF/X-4`
  - Image: raw CMYK bytes (FlateDecode compressed) with `/ICCBased` colorspace referencing FOGRA39 — avoids CMYK JPEG APP14 byte-inversion bug
- Downloads as `<project-name>.pdf` (uses project name field)
- **Confirmed 2026-07-24, at Julia's request:** re-verified this whole chain end-to-end against the actual code (not just re-reading this doc) — the `.icc` file exists on disk and is a valid CMYK printer profile, `withIccProfile()` genuinely does the conversion, and the same profile is embedded in the output PDF both as the OutputIntent and as the image's real `/ICCBased` color space. FOGRA39 is the only profile ever used — the right-panel "ICC Profile" field previously showed a dropdown with GRACoL/SWOP/Japan Color options, but that `<select>` had no `value`/`onChange` at all, so picking a different option changed nothing (the export always used FOGRA39 regardless). Since the only client is Wolt DE (Germany), those other options were never relevant anyway — replaced the dead dropdown with a plain fixed "✓ FOGRA39 (European offset)" row so the UI doesn't imply a choice that was never real.

---

## All bugs fixed

| Bug | Fix |
|-----|-----|
| FUNCTION_PAYLOAD_TOO_LARGE on save | Client-side image compression (max 1500px, JPEG 82%) before sending |
| "Cannot use public access on a private store" | Changed `access: 'public'` → `access: 'private'` in save-project.js; added server-side load-project.js |
| "This blob already exists" on re-save | Added `allowOverwrite: true` to save-project.js and add-comment.js |
| Comments not visible on re-open | Fetch comments directly in handleOpenProject (not via useEffect) |
| Sub-headline font size resets on re-open | `loadKey` prop resets prevFieldsRef before auto-shrink comparison fires |
| Initial canvas shrink ignores saved font sizes | Shrink-to-fit pass now starts from `fontSizesRef.current` not `zone.fontSize` |
| T&C position + font size resets on re-open (designer) | Zone positions (left/top/width) saved in project JSON and restored on canvas init |
| Delete from Designs only cleared localStorage | Delete button now also calls `DELETE /api/delete-project` (removes project + comments from Blob) |
| **PDF colors completely wrong (near-black)** | CMYK JPEG carries an APP14 "Adobe" marker that inverts byte values (0=full ink); PDF viewers read it un-inverted, causing near-black. Fixed by converting to FOGRA39 CMYK via `withIccProfile`, extracting raw bytes, and compressing with FlateDecode — no JPEG encoding, no convention ambiguity. |
| **Sub-headline resets slightly on re-open** | Auto-shrunk font sizes were computed at runtime but never saved — only manual +/- overrides were persisted. On each re-open font-loading timing produced slightly different shrink results. Fixed by saving the canvas's actual displayed font sizes (`getEffectiveFontSizes()`) into the project JSON. |
| **Image scale (photo/logo) not restored on re-open** | `imageScales` effect ran before `fabric.Image.fromURL` callback completed, so `_wcBaseScale` wasn't set yet. Now applies saved scale immediately inside the image load callback. |
| Duplicate "Print settings" heading in right panel | Removed extra copy-paste heading from FieldEditor. |
| **Text left-aligned on re-open in guided mode** | Designs saved while in designer mode stored explicit `left` alignment values in the project JSON. On re-open in guided/non-designer mode (which has no alignment controls to correct it), those stale values overrode the template's `align: 'center'` zone config. Fixed by making `addZones()` and the alignment sync effect always use `zone.align` in non-designer mode, ignoring any saved alignment overrides. Also added `alignmentsRef` so the async canvas-init closure always reads the latest alignments. |
| **Canvas renders with wrong fonts on first production load** | `document.fonts.ready` only signals that `@font-face` declarations are parsed — the actual Omnes font files are not yet downloaded. On first production load (no cache), the canvas rendered with system fallback fonts, causing text to appear wrong and mis-sized. Fixed by adding `document.fonts.load()` calls for each Omnes weight used on the canvas; `addZones()` now only runs after the font files are confirmed downloaded. |
| **Re-saved project changes not reflected on re-open** | Three caching layers were all stacking: (1) browser cached `/api/load-project?url=...` since the URL never changed — fixed by adding `&_t=Date.now()` + `cache:'no-store'` to the browser fetch; (2) Vercel edge cached the API response — fixed by adding `Cache-Control: no-store` response headers in `load-project.js`; (3) Cloudflare CDN in front of Vercel Blob served stale blob content — fixed by appending `?_t=Date.now()` to the Blob URL inside the server-side fetch. Also added sessionStorage write-on-save / read-on-open to bypass the network entirely for same-session re-opens. |
| **Font sizes revert on project re-open (same template)** | `TemplateCanvas` received `loadKey` as a regular prop, not as a React `key`. Without `key={loadKey}`, the component never remounted when reopening the same template — the canvas init effect (which depends only on `[config]`) never re-ran because `TEMPLATE_ZONES[templateId]` is a constant reference. Stale Textbox objects from the first init kept their original font sizes. The `[fontSizes,config]` sync effect ran but fought the stale canvas state. Fixed by adding `key={loadKey}` to `<TemplateCanvas>` in App.jsx, forcing a full remount on every project open or template selection. |
| **Sub-headline (and other guided-mode zones) reverts to wrong size on re-open after manual size bump** | The locked-mode auto-shrink block in `addZones()` always re-ran from the saved font size as its starting point. If the user had manually bumped the size above what auto-shrink calculated (e.g., auto-shrink → 12pt, user bumped to 15pt, saved 15pt), the reload would start at 15pt, detect overflow (because 15pt was always too large), and shrink back to 12pt — silently undoing the user's manual change. The thumbnail captured at save showed 15pt; the canvas on reload showed 12pt. Fixed by skipping the shrink loop entirely when a saved font size exists — saved size is the source of truth. Auto-shrink from the zone default only runs on first-ever open of a fresh template with no saved state. |
| **Dead code cleanup** | Removed 4 files never imported anywhere: `PreviewCanvas.jsx`, `PdfThumbnail.jsx`, `src/lib/pdfSetup.js`, `api/generate-pdf.js` (legacy export route superseded by export-cmyk.js). Also uninstalled unused `pdfjs-dist` npm package. Build verified clean (27 modules, 0 errors). |
| **Print test (2026-07-13): frame cut off, no real bleed** | `export-cmyk.js` stretched the trim-size canvas render (`fit:'fill'`) across the *entire* bleed-inclusive page, pushing edge content — including the frame border — outward past the TrimBox line. The physical printer then trimmed 3mm off each side per the TrimBox and sliced the frame off. Fixed by resizing to the true trim pixel size (1241×1749px, no distortion) and using `sharp`'s `.extend({..., extendWith: 'mirror'})` to add a genuine 35px (3mm) bleed margin by mirroring the trim-edge pixels outward — all design content now stays registered exactly at the trim line. Verified locally that output dimensions land exactly at 1311×1819px. Not yet re-verified against an actual physical print run. |
| **Logo Position nudge did nothing on any template (2026-07-24)** | `clampOffset()` computed slack as `Math.max(0, (scaledDim - zoneDim)/2)` — correct for cover-fit photos (which always overflow the zone) but always zero for contain-fit logos, which fit *inside* the zone at 100% scale (underflow, not overflow). Fixed by using `Math.abs(scaledDim - zoneDim)/2`, which gives real slack in the letterboxed axis for contain-fit images too. Verified in-browser (designer + guided mode, both templates) via synthetic test images injected through React fiber + pixel-level canvas sampling — nudge now moves the image exactly the expected 4px per click, still correctly clamped to 0 on any axis with zero slack. |
| **Option A: restaurant name not inline with baked "♥ WOLT" (2026-07-24)** | Pixel-scanned the live canvas: `restaurant_name`'s own baseline sat 4px above the baked wordmark's baseline. Nudged `WEN_CHENG_V3_ZONES.restaurant_name.y` from 149→153. Verified: both baselines now land on the same canvas row. |
| **Option B: food photo couldn't overlap the headline (2026-07-24)** | Photo zone had no `overlapAbove`, and even after adding it, the z-order fix silently no-op'd due to a scoping bug — the effect referenced a bare `zones` variable that doesn't exist in that closure (only `config` does), throwing a swallowed `ReferenceError` inside the async image-load callback. Fixed the reference (`config.zones`) and made the reorder idempotent across zones/async load order (previously only the *current* zone's overlap image got re-fronted, so re-uploading e.g. the logo after the photo would silently un-overlap it). Verified via solid-color test image injected through React fiber — photo now renders continuously on top of the entire headline zone. |
| **Option B: CTA size was 11pt, spec calls for 10pt (2026-07-24)** | `OPT_B_ZONES.cta.fontSize` 11→10 (already correct Omnes Pro Semibold 600). |
| **Position nudge "stops working after ~3 attempts" (2026-07-24)** | Reproduced live, not just theorized: the clamp that prevents an image from being nudged far enough to reveal empty background is working exactly as intended, but for a near-exact-fit logo the total available range can be as little as ~8px — 2 clicks at 4px each — with zero visual feedback when you hit it. Not a logic bug; a missing-feedback UX problem. Added a plain-language hint under the Position control ("bump Scale up first for more room to nudge"). |
| **Scale steps looked like arbitrary numbers (2026-07-24)** | Steps were 10% from a 100% base, which is clean, but Julia wanted finer 5%-increments (70/75/80…) — changed the step size and disabled +/- at the 20%/300% hard bounds instead of leaving them silently inert there. |
| **No visual trim-line indicator in the editor (2026-07-24)** | The canvas only ever renders the trim-size art (bleed is added later at export, by mirroring the trim edge outward) — there was no marker at all for where the physical cut line sits. Added a thin red rect around the canvas perimeter, hidden on export like the other guides. |
| **Export frame looked bigger/different than the Figma master — first pass (2026-07-24)** | Initial investigation concluded the export was correctly sized and the extra margin was just the real 3mm bleed. **This was wrong/incomplete** — see the follow-up row below, which found and fixed the real cause after Julia pushed back with an actual InDesign-vs-WildCast side-by-side. The one genuine (if minor, ~1%) thing fixed in this pass stands: the editor canvas (316×441px) is ~1% off true A6's 105:148 ratio, so export's `fit:'fill'` resize was stretching every element slightly non-uniformly — switched to `fit:'cover'`. |
| **Export frame really was too small — real root cause found (2026-07-24)** | Measured precisely against Figma's own screenshot API (not a screenshot comparison by eye): the Figma master's teal-box margin is 6.35%/4.58% of the full bleed frame; a freshly-generated WildCast export measured 8.38%/6.37% — both numbers land almost exactly on "correct margin + one extra 3mm bleed." **Root cause:** `api/_lib/figma-import.js` exported Figma's FULL bleed frame (111×154mm) as the background PNG, but every zone coordinate — and the whole editor/export pipeline — treats that background as trim-only (105×148mm, no bleed). So every imported background carried an extra ~3mm margin the zone math didn't know about, and export-cmyk.js's own mirror-bleed-extend added a SECOND 3mm on top: effectively double bleed, silently shrinking every design ~4-6% smaller than the true master. Fixed by cropping the fetched image by the bleed margin (scaled to the export's pixel scale) before returning it, so the saved background genuinely is trim-only. Fixes every *future* import — does **not** retroactively fix the already-live Option A/B backgrounds, which are static files baked by the old buggy code; regenerating them needs a working Figma token (Claude's local one expired, same issue Julia hit) — flagged to Julia to provide one. |
| **Published import "nothing appears anywhere" (2026-07-24)** | Two real, previously-flagged-but-never-exercised bugs, both confirmed and fixed: (1) `api/list-templates.js`/`api/publish-template.js` read the record blob with no cache-busting — the same Cloudflare-CDN-staleness bug already hit and fixed for save/load-project, just never ported here, so a publish could flip `live:true` while the next list-templates read still served the stale pre-publish version. Fixed with the same proven pattern (timestamped blob reads, no-store response headers, no-store client fetch). (2) The background is a private Blob URL used directly in `<img src>`/`fabric.Image.fromURL` with no proxy — confirmed directly in Julia's own screenshot (the draft-review card showed no image at all). Added a `?url=` proxy branch to `list-templates.js` (same pattern as `library-assets.js`) via a new `templateAssetSrc()` helper, applied to the catalogue thumbnail, canvas background, and draft-review preview. Verified against Julia's real existing draft on production: proxy now correctly returns the real PNG (200, image/png). |
| **Draft/publish flow wasn't reassuring enough (2026-07-24)** | The existing "Draft" badge + Publish button were easy to miss (especially with the background not rendering, per the bug above — the whole card looked broken). Added an explicit pop-up right after import: "Saved as a draft — not visible to partners yet," with a clear **Keep as draft** / **Publish now** choice. |
| **No real bleed margin shown in the editor (2026-07-24)** | The earlier red trim-line guide marked the trim edge but showed nothing beyond it — Julia asked for exactly what the Figma master shows: a visible bleed margin around the trim line. Wrapped the canvas in a CSS-only bleed margin (9px ≈ 3mm at canvas scale) with a solid outer border and white background, dashed trim line sitting just inside it — purely a visual wrapper outside the actual `<canvas>`, zero change to canvasW/canvasH or any zone coordinate. |

---

## Figma import pipeline (new 2026-07-13)

Real templates can now be pulled straight out of Figma instead of manually pixel-scanning exported PNGs.

- **`wildcast-app/scripts/import-figma-template.js`** — run with `node --env-file=.env.local scripts/import-figma-template.js <figma-frame-url> <output-name>`. Fetches node geometry via Figma's REST API (`/v1/files/:key/nodes`) and a print-resolution background export (`/v1/images/:key?scale=4` — Figma caps image export at 4x/288 DPI server-side, just under the 300 DPI target but print-fine) via the same API. Outputs a background PNG into `public/templates/` and a ready-to-paste zone array into `scripts/*-zones.generated.js` (gitignored — copy the values into `templateZones.js` by hand).
- **Convention the script depends on:** any Figma node meant to become an editable WildCast zone must be named `zone:<id>` (e.g. `zone:headline`, `zone:logo`, `zone:photo`, `zone:cta`). Nodes named `zone:logo`/`zone:photo` become image zones (fit contain/cover); everything else becomes a text zone, with real font family/size/weight read automatically **only when the zone marker itself is a text node** — a placeholder shape (rectangle) gives correct position/size but no font data, which then needs manual completion.
- **Master template requirement:** the target Figma frame must be the bleed-size master (111×154mm @ 3mm bleed), with guide/placeholder layers set to `visible: false` as their *permanent resting state* in the file — the image export renders whatever is currently visible in the saved file, there's no way to toggle visibility live via the REST API the way the interactive Figma plugin can.
- **Needs a Figma personal access token** — each person who runs the script generates their own (Figma Settings → Security → Personal access tokens → scope `file_content:read` only), saved in `wildcast-app/.env.local` (`FIGMA_TOKEN=...`, gitignored, never shared).
- **First two templates brought in through this pipeline (2026-07-13):**
  - **Option A (Wen Cheng)** upgraded in place — real bleed-corrected background + script-derived zone positions, same template IDs as before. Caught and fixed a real regression risk during this: the Figma master still had "WEN CHENG" baked in as static text, which would have undone the already-shipped per-partner editable restaurant name feature. Masked it out and added a proper `zone:restaurant_name` marker instead. Also fixed sub_headline/headline/offer zones overlapping each other by 15–23px — the first pass used the raw text nodes' own bounding boxes (which reflect placeholder-text render height, not designed spacing) instead of their dedicated non-overlapping guide rectangles, same pattern already used correctly for logo/photo.
  - **Option B replaced entirely** with a McDonald's Q4 Koblenz-based flyer — the first template built through the new pipeline from scratch. The line naming McDonald's specifically was masked out of the background art and replaced with a free-text `cta` zone so any partner can fill in their own line.
- **Known Figma quirk hit during this work:** a cloned decorative vector (30pt white stroke, used for a card border effect) rendered with its stroke missing in one specific clone despite every measurable property (position, fill, stroke, z-order) being byte-identical to a working clone of the same source vector elsewhere in the file. Root cause not identified even after extensive diagnostic comparison — worked around by falling back to the simpler background construction (inset colored rect + white base fill) that was already proven reliable, rather than keep chasing it.
- **✅ Superseded (2026-07-13, same day) by a self-serve in-app import** — see below. The CLI script above still works and is what the in-app feature is built on top of, but you no longer need to run it by hand for a new template.

---

## Self-serve Figma import (new 2026-07-13)

The manual script above now has a real in-app front end — paste a Figma link into WildCast itself, no code edit or redeploy needed to bring in a new template.

- **How it works:** an "Import" nav item (visible only to `role:designer` activation keys) opens a form — Figma frame URL + target catalogue slot (any currently "coming soon" slot, e.g. "Restaurant Flyer · Option C"). Submitting calls `POST /api/import-figma-template`, which pulls zones + a real bleed background from Figma (same logic as the CLI script, now shared via `api/_lib/figma-import.js`) and saves both to Vercel Blob under `templates/<slotKey>.json` / `templates/<slotKey>-bg.png`.
- **Draft-first, always.** An import never goes live automatically — it lands with `live:false`. The designer reviews the result (background + zone list) in the same screen, then clicks **Publish** (`POST /api/publish-template`) to make it real. This is deliberate: a bad import can never surface to a real partner without a human choosing to publish it.
- **Templates stay hybrid, not fully migrated.** Option A and B are still hardcoded in `templateZones.js`/`templates.js` exactly as before — untouched, zero migration risk. Only *new* imports go through Blob, and they can only fill *empty* catalogue slots (`BASE_TEMPLATES` in `TemplatePicker.jsx` — the 30-slot skeleton never changes, just which slots have real data). `App.jsx` fetches `/api/list-templates` once on load and merges the result with the static templates; if that fetch fails for any reason, the app just falls back to the static templates silently.
- **New role field on `WILDCAST_KEYS`** — see Activation key system below. Defaults to `partner` so every existing key keeps working with zero changes.
- **Figma token is now managed in-app, not just a Vercel env var (2026-07-24)** — see the dedicated section below. `FIGMA_TOKEN` (Vercel env var) still works as a fallback for a brand-new deploy that's never had a token set via the app.
- **✅ 2026-07-24: real end-to-end test happened** (Julia imported + attempted to publish a real "Restaurant Flyer · Option C" draft) and found two real bugs — see the "Published import 'nothing appears anywhere'" bug-table row above. Both fixed and confirmed against her actual production draft.
- **✅ 2026-07-20 live check:** confirmed via the deployed JS bundle that all of the above code (import route, list/publish/delete routes, `restaurant_name` zone, v3 backgrounds) is genuinely live in production — Vercel auto-deploy from GitHub is working correctly. The "Import" nav item is still invisible on the live site because the `role` field has never actually been added to `WILDCAST_KEYS` in Vercel (confirmed by logging in live with `WILD-Demo-KEY` — nav shows Templates/Library/Designs/Help, no Import). This is a one-time Vercel dashboard edit, not a code issue — see Activation key system below.
- **Key files:** `api/_lib/figma-import.js` (shared extraction logic), `api/import-figma-template.js` / `api/list-templates.js` / `api/publish-template.js` / `api/delete-template.js`, `src/lib/customTemplates.js` (static+dynamic merge), `src/components/TemplateImportPage.jsx`.
- **✅ Fixed 2026-07-24** (was flagged 2026-07-21 as a likely latent bug): `backgroundUrl` was a raw private Blob URL used directly in `fabric.Image.fromURL`/`<img src>` — confirmed broken the first time a real import actually went through (Julia's draft-review card showed no image). Fixed via a `?url=` proxy on `api/list-templates.js` + `templateAssetSrc()` helper in `src/lib/customTemplates.js`, same pattern as `api/library-assets.js` below.
- **⚠️ New, related, not yet fixed:** the background PNG itself was also found to carry an extra ~3mm of unaccounted bleed margin (see "Export frame really was too small" in the bug table above) — fixed in `api/_lib/figma-import.js` for all *future* imports, but Option A/B's already-live static backgrounds still have the old, incorrect margin baked in. Needs a working Figma token to regenerate them via `scripts/import-figma-template.js`.

### Real end-to-end import test on "Restaurant Flyer · Option C" surfaced 4 real bugs (2026-07-28)

Julia's first real (non-Option A/B) import went through the whole pipeline for the first time — surfaced problems the earlier "coming soon" testing never exercised, since Option C is the first template with placeholder-shape zone markers, a separate real-text sibling layer, and a rotated fine-print zone.

- **Canvas stuck on "Loading canvas..." forever, no error shown.** Root cause, confirmed via a live JS stack trace (reproduced in-browser, not guessed): a zone whose Figma marker wasn't itself live text (`_needsFontReview`) saved `fontFamily: null` — Fabric's Textbox throws constructing/measuring text with a null fontFamily ("Cannot read properties of null (reading 'toLowerCase')" deep in its font-cache code), an uncaught exception mid-loop in `TemplateCanvas.jsx`'s `addZones()`, so `setLoading(false)` at the end of that function never ran. Fixed with real fallback values (`fontFamily: 'omnes-cond'`, `fontSize: 24`) instead of null, both at the point of use and at the source in `figma-import.js`.
- **Headline/Sub-headline position and font completely wrong.** Root cause, found by reading the real Figma file's layer data directly (`mcp__Figma__get_metadata`): Julia's file has `zone:headline`/`zone:sub_headline` as separate position-only marker boxes, with the actual styled text sitting in plain-named sibling layers (`headline`/`sub_headline`, no "zone:" prefix) that don't share the marker's exact box. `toCanvasZone()` only ever read geometry+style from the `zone:<id>` node itself. Now searches the whole frame for a plain-named TEXT layer matching the zone id when the marker itself isn't text, and uses that layer's real box + style instead — a normal, valid Figma workflow (separate boundary marker + actual content) the importer previously couldn't handle.
- **T&Cs never rotated — a gap in the pipeline, not this file specifically.** `tc` has no live text anywhere in this Figma file to sample from at all, but by app-wide convention (confirmed against Option A) it's always the narrow rotated fine-print sidebar. The importer had never captured rotation from Figma at all. Added a small `ROTATED_TEXT_DEFAULTS` table (currently just `tc`) that sets `rotate: -90` + a matching `textWidth` even with zero live text to go on.
- **Every imported heading rendered ~5% larger than its real Figma size.** `fontSize` was being multiplied by the same `scaleY` factor used for position/dimensions (~1.05 for this file) — correct for spatial coordinates, which genuinely need remapping from Figma's frame space into the fixed 316×441 canvas space, but wrong for a font's point size, which should read the same as what a designer sees directly in Figma's own text panel. Caught by comparing Julia's exact spec (headline 51 / sub-headline 35) against the imported 54 / 37 — precisely the scaleY-multiplied values once rounded, not a coincidence. `fontSize` is no longer scaled at all now.
- All four fixed and reverified live against Julia's real draft (re-imported after each fix, not just claimed) — confirmed via `GET /api/list-templates` showing the corrected zone JSON, and by opening the actual canvas via a direct React-fiber `onSelect` call (no UI workaround needed) to visually confirm rendering.
- **Also found and fixed in the same investigation:** the in-app "Update token" flow had the exact same Cloudflare-CDN staleness bug already hit and fixed elsewhere in this file (`list-templates.js`, `publish-template.js`) — a freshly-saved Figma token could still read back the old cached value on the very next import. `resolveFigmaToken()` in `api/import-figma-template.js` now cache-busts the same way.

### Designs go global + categorized, Import list organized by group (2026-07-29)

- **Designs were never actually global** — confirmed by reading the code, not assuming: the list of "which designs exist" lived entirely in a per-browser `localStorage` registry. Even though each design's content was already saved server-side, nobody but whoever saved it, on that exact browser, ever knew it existed. Julia confirmed this should change (over keeping Designs per-browser but better organized).
- **`api/save-project.js` gains a GET (list) handler** alongside the existing POST (save) — still 12 total function files. Lists every saved project straight from Blob, computing a `merchant` tag server-side (real restaurant name if the template has one, else the project's own name).
- **`DesignsPage.jsx` rewritten**: fetches the global list; derives each design's format (Restaurant Flyer, Retail Poster, etc.) from the same `cat`/`format` fields every template card already carries — no new data needed on a saved project itself. Shows a **"Find a design" popup** (Format + Merchant dropdowns, both default to "All") before any design renders — Julia's ask for a structured pre-filter gate rather than just an inline search box — then groups results by format below it, with a "Filters" button to reopen and adjust. Verified live: the popup correctly listed real merchants from 14 existing saved designs, and grouped them correctly after applying.
- Removed the now-dead `localStorage` write in `App.jsx`'s save flow (the separate `sessionStorage` same-session-reopen cache is untouched).
- **`TemplateImportPage.jsx`'s "Previously imported" list is now grouped into collapsible sections** by category + format (same grouping Templates already uses), instead of one flat list that would only get harder to scan as more templates get imported.
- **Reverted same day, per Julia's own follow-up call**: archived templates stay visible with the "Archived" label + Restore action right on the Templates catalogue page (not hidden, and not exclusively on the Import screen as briefly tried) — she tried both and preferred having it in both places.

### Duplicate-or-edit-original safety net for Designs (2026-07-29)

- **Risk surfaced by going global**: once every activation key can see and open every saved design, there's no ownership boundary — anyone can open, edit, and overwrite anyone else's saved work. Julia's fix: ask first.
- Opening a design from the Designs tab (card click or "Continue editing") now shows a popup: **"Edit original"** (unchanged behaviour — opens and, on next Save, overwrites that exact record) or **"Duplicate & edit a copy"** (clones the full saved state under a brand-new id, saves the clone immediately as its own independent record, then opens the *copy* — the original is never touched).
- `App.jsx`: `handleOpenProject` split into `loadFullProject` (sessionStorage-then-fetch) + `openLoadedProject` (populate editor state), so both the original-opening path and the new `handleDuplicateProject` path share one implementation instead of two copies of the same logic.
- No new API route — the duplicate is created with the same `POST /api/save-project` every regular Save already uses, just called immediately with a cloned payload.
- **Testing this surfaced a real, pre-existing bug**: opening almost *any* older saved design crashed the whole app blank (`Cannot read properties of undefined (reading 'length')`). Cause: `FieldEditor.jsx`'s character-limit counter reads `value.length` unconditionally for `headline`/`offer`/`sub_headline`/`tc`/`restaurant_name`/`cta`; opening a project did `setFields(project.fields ?? DEFAULT_FIELDS)` — a full replace, not a merge — so any design saved before a field like `cta` existed loaded with that key simply missing, not empty-string, and crashed on render. Fixed by merging onto `DEFAULT_FIELDS` instead of replacing it (`{ ...DEFAULT_FIELDS, ...(project.fields ?? {}) }`), so older saves fill in gaps with sane defaults. Confirmed live: reproduced the crash on multiple existing designs (including a normal "Restaurant Flyer · Option B" save, not just test data), fixed, then confirmed both Edit-original and Duplicate open cleanly afterward.

### Import's "Previously imported" groups now actually collapse (2026-07-29)

- The prior day's grouping used `<details open>` with the summary marker suppressed — every group rendered permanently expanded with no visible arrow, so it read as one long list again once there were more than a couple of imports, not the tidy accordion it was meant to be.
- Replaced with a controlled accordion (`openGroups` state, all collapsed by default): each group header is a real button with a chevron (▶) that rotates on toggle, and only an expanded group's records render at all.
- Archived-but-visible templates (Option C/D, per Julia's own earlier call to keep them shown with a Restore action) are unaffected — that's the Templates catalogue page's intended, confirmed behaviour, not a bug; this change only touches the Import screen's list layout.

### Option D test found 3 more real bugs, then a deploy incident briefly took production down (2026-07-28)

- **Publish/Archive looked like it needed 2-3 clicks.** Real cause: every action handler refetched to show the new status, but that refetch reads through the same Vercel Blob `list()` consistency lag noted above — the first click's refetch could still show the pre-action status. Fixed by applying each action's own response (already known-good) straight to local state instead of waiting on a refetch that might lag behind.
- **T&Cs auto-shrink was completely disabled for any rotated zone**, on every template including Option A/B — masked until now because their content happened to already fit. A rotated zone's pre-rotation height becomes the visual *thickness* once drawn at -90°, so the fit check needs `zone.width`, not `zone.height`. Fixed properly instead of skipping rotated zones.
- **No way to preview a draft before publishing it** — the catalogue's draft tile had no click handler at all. Designer-role users can now click a draft tile to open it in the normal editor, same as a live card; still invisible to non-designer users.
- **A real archived custom template had no way back — its own menu still showed Publish/Archive instead of Restore.** The archived-check only applied to the hardcoded Option A/B override records, not real archived Figma imports. Fixed to check `archived` first, unconditionally.
- **Deploy incident (self-inflicted, disclosed plainly):** while investigating why a push seemed stuck, a stale local Vercel project link (silently pointing at an unrelated, unconfigured project of the same name) led to a manual deploy that briefly took the real site down — `/api/validate-key` and other routes 404ing — for a couple of minutes before being caught and reverted. Root cause and full account in this session's Claude memory (`project_vercel_deploy_safety`). Every push before and after this deployed correctly via the normal automatic git-push flow; this was not a recurring platform problem.
- All fixes verified live post-deploy via direct DOM/API inspection.

### Making imports more self-correcting (added 2026-07-28)

Julia asked for a way to "pin down" the kind of pixel-level corrections above without needing a code change + chat round-trip every time, plus two QR-specific asks (auto-fit, merchant tagging).

- **"Zone settings" review panel** — `TemplateImportPage.jsx`'s result panel (shown right after a fresh import) now lists every text zone with an editable font-size input and a "Rotate 90°" checkbox, saved via a new `updateZones` action on `api/publish-template.js` (full zones-array replacement, no new function file needed — still 12 total). A designer can now fix a font size or flip a rotation immediately after import, no code change or redeploy required. Scoped to the just-imported record only (tied to the transient import result, not re-openable later for an older draft — re-import again if needed, which is safe/idempotent).
- **QR auto-crop on upload** — many QR generators export with a big white "quiet zone" margin baked into the file, which showed as visible gaps around the code even though contain-fit/scale/position were all working correctly. `src/lib/image.js`'s new `cropToContent()` scans a downsampled copy of any QR upload for the tightest bounding box of non-white/non-transparent content and crops to it before placing — partners never need to manually crop or resize their own QR export.
- **Merchant tagging fallback for templates without `restaurant_name`** — previously always landed in a generic "General" folder when a template had no restaurant-name field to key off (e.g. Option C). Now falls back to the Project Name field instead, still zero extra clicks for the partner.
- **Real gotcha hit while testing `updateZones`:** rapid back-to-back writes to the exact same blob key (import → save → save again, all within under a minute) can take up to ~30s for a subsequent `list()`-based read to reflect the latest write — a genuine Vercel Blob storage-consistency characteristic, not a caching bug the existing `_t=` cache-busting can solve (that only defeats Cloudflare's CDN layer, not backend replication lag). Confirmed self-resolving (not a data-loss bug) via a direct curl-only repro with no browser involved. Not a concern for normal one-edit-then-move-on usage — only surfaced under stress-testing rapid successive saves.

### Archive a template + safer re-import (added 2026-07-28)

Julia asked for a way to archive an imported template, while separately noting an existing draft ("Restaurant Flyer · Option C", imported the day before) needed re-importing. Investigating surfaced a real gap alongside the explicit ask: `TemplateImportPage.jsx`'s target-slot dropdown read emptiness off `BASE_TEMPLATES`' own static `live:false` — which never changes for a custom slot — so a slot that had already been published via a custom record would still show up as a valid import target, risking an accidental overwrite of something partners currently see. Fixed as part of this feature, not left for later.

- **`api/publish-template.js` is now action-based** (still one file, no new function slot needed): `POST {slotKey, action}` where `action` is `publish` (→ live), `unpublish` (→ back to draft, still publishable), `archive` (→ hidden everywhere, frees the slot for a fresh import), or `restore` (→ back to draft, does *not* auto-publish).
- **`archived` is a real field now**, threaded through `src/lib/customTemplates.js`'s `customTemplateCards()` and checked in `TemplatePicker.jsx`'s `overlayCustomCards()` — an archived record renders exactly as if the slot had never been imported into, both in the catalogue and hero briefing flow.
- **`TemplateImportPage.jsx`'s target-slot dropdown now excludes any slot with a currently-*live* custom record** (draft and archived slots stay valid re-import targets — re-importing over an unpublished draft, like Option C, or a previously-archived slot, just overwrites it, exactly as before).
- **"Previously imported" list rebuilt** with a real three-state badge (Live / Draft / Archived) and per-record action buttons (Publish, Unpublish, Archive, Restore) — previously this list was read-only with no way to act on a past import at all.
- Archiving a currently-*live* record asks for an inline confirmation first (it's immediately partner-visible-to-invisible) — restoring never auto-publishes, so getting a slot back is always a deliberate second step.
- **On-card manage menu (same day, follow-up).** Julia found the Import screen's list wasn't where she wanted to manage things from — she's naturally browsing the Templates catalogue when she notices something's wrong with a specific card. Added a designer-only "⋯" menu directly on each Figma-imported card in `TemplatePicker.jsx`'s `OptionsView` (Publish/Unpublish/Archive), gated on `activation.role === 'designer'` (threaded via a new `canManage` prop from `App.jsx`). Draft (unpublished) custom records also now render distinctly — "Draft — not published" with a coral dashed border — instead of being indistinguishable from a truly-empty "Coming soon" slot, which was itself a real gap (no way to visually tell the two apart before this).
- Verified live: `GET /api/list-templates` on the deployed app confirmed the "Restaurant Flyer · Option C" record correctly reached `{live:false, archived:true}` after Julia used the feature herself.

### Figma access token — now managed in-app (2026-07-24)

Julia hit a real `403 Token expired` from the Figma API even though her token (visible in Figma's own settings) wasn't actually expired yet — the real problem was that Vercel's `FIGMA_TOKEN` env var held an *old* token value that no longer matched her current one, and fixing that meant a Vercel dashboard visit + redeploy. Since these tokens expire every few months, that dependency on a developer was the actual complaint.

- `api/import-figma-template.js` (already at the 12-function limit, so no new file) gained `GET` (returns `{configured, source, updatedAt}` — never the token value itself) and `PUT` (saves a new token) handlers alongside the existing `POST` import handler.
- Token is stored in Vercel Blob (`config/figma-token.json`, private) via a shared `resolveFigmaToken()` helper — checked first on every import, falling back to the `FIGMA_TOKEN` env var only if nothing's ever been saved to Blob yet. A brand-new deploy still works via the env var exactly as before; setting a token in-app just takes over from there.
- New "Figma access token" panel at the top of the Import screen (`TemplateImportPage.jsx`) shows configured/not-configured + last-updated date, with a password-masked field to set or replace it — no code, no redeploy, no Vercel dashboard needed going forward.

---

## Asset library (rebuilt 2026-07-21 — Vercel Blob, not localStorage)

The library used to be 100% client-side localStorage, capped at 800px per image to fit the ~5-10MB browser storage ceiling — fine for browsing thumbnails, but meant anything reused from the library was well below print resolution, and the library was siloed per-browser. Rebuilt on Vercel Blob (Julia's choice, over keeping it browser-only) — same architecture as saved projects/templates.

- **One route, `api/library-assets.js`**, dispatched by HTTP method: `GET` (no query) lists everything, `GET ?url=<blobUrl>` proxies a single private blob's bytes to the browser, `POST` uploads, `DELETE ?url=<blobUrl>` removes. All three original separate files got merged into this one — see the function-count gotcha below.
- **Data model:** no metadata JSON per asset — folder + display name are encoded directly in the blob's pathname (`library/<folder>/<uuid>__<name>.<ext>`), `uploadedAt` comes from Blob's own list() metadata.
- **Frontend:** `src/lib/assetLibrary.js`'s `getLibraryAssets`/`saveAssetToLibrary`/`deleteLibraryAsset` are now async (fetch-based). Every asset gets a `.src` field (the proxied path) — always use `.src` for rendering/pixel-reads, `.url` (the raw private blob URL) only for delete.
- **Direct upload UI added to the Library page itself** (previously the only way in was auto-save from inside a template editor) — four buttons now (logo / product photo / sticker-badge / QR code), same `hasTransparency()` check the canvas already enforces per zone type, plus a resolution badge per asset (general ≥1200px-on-the-long-side heuristic, since there's no specific print zone to check against on this page).
- **Rename support (2026-07-21)** — click an asset's name on the Library page to rename it in place. No in-place rename in Vercel Blob, so this `copy()`s to a new pathname (same folder/id, new name) then `del()`s the old one — folded into `api/library-assets.js` as a `PATCH` to avoid a new function file. Added because several QR codes look visually identical and are otherwise impossible to tell apart.
  - **Gotcha hit + fixed:** the sanitization regex used when building the blob pathname (`name.replace(/[^a-zA-Z0-9._-]/g, '_')`) stripped spaces, so a rename to "Munich Store QR" round-tripped as "Munich_Store_QR" on the next list refresh (display name is derived from the pathname, not stored separately) — defeats the point of a human-readable rename. Relaxed to only strip characters genuinely unsafe in a URL path segment (`/ \ ? % * : | " < >`), spaces and most punctuation now survive.
- **Known limitation:** old localStorage-saved assets do not migrate automatically — different storage system. Acceptable given this is still early/low-volume usage, not real client data.
- **⚠️ Two real Vercel gotchas hit and fixed while building this — worth knowing before adding any future `api/*.js` file:**
  1. **Serverless function count limit.** This project's Vercel plan caps a deployment at 12 functions. Adding 3 new one-per-route files (15 total) made the deploy **fail silently** — build logs look clean, it just dies right after "Deploying outputs..." with no code-level error anywhere in the CLI. Only found via `gh api repos/.../commits/<sha>/status` + `vercel inspect <deployment-id> --logs`. **Before adding a new API file, run `ls api/*.js | wc -l` — if already near 12, merge into an existing file (dispatch by `req.method`) instead.**
  2. **The Blob store is private-access-only.** `put(..., {access:'public'})` is flatly rejected: `"Cannot use public access on a private store."` Any binary asset needs the same proxy-through-a-server-route pattern as private JSON already uses elsewhere (`load-project.js`) — a plain `<img src>` on the raw blob URL will not work.
- **Verified end-to-end live**, not just build-clean: uploaded a synthetic 1600×1200 test PNG directly via the API, confirmed it lists correctly, proxies back at full resolution, and renders/applies correctly both on the Library page and inside a template's "choose from library" picker. Cleaned up the test asset after.

### Merchant folders + search (added 2026-07-24)

Wolt DE is the only client, but they have many merchants (restaurants) — the shared library had no way to separate one restaurant's assets from another's, and "choose from library" had no way to find anything once a folder grew past a handful of items.

- **Pathname gains a merchant segment:** `library/<folder>/<merchant>/<id>__<name>.<ext>` — defaults to `"General"` when no merchant is given (shared/non-merchant assets, e.g. a generic Wolt app-store badge). `api/library-assets.js`'s GET/PATCH handlers parse both this new 4-segment format and the older 3-segment (no-merchant) format for backward compatibility — old assets just show under "General" rather than needing a migration script. Renaming an old asset also migrates it into the merchant-scoped format.
- **Library page:** a "Viewing" dropdown filters the asset grid below by merchant (or "All merchants", showing everything with a small merchant tag per card). A search box filters by name within whatever view is active. Last-used view remembered via `localStorage`. **(Originally this same dropdown also silently decided which merchant a new upload got tagged with — see the UX fix below; it's now purely a view filter.)**
- **In-canvas "choose from library" is now a real pop-up modal** (was an inline-expanding grid), with its own search box and a merchant filter dropdown (only shown when the folder actually has more than one merchant). **Defaults to whichever restaurant name is currently typed in the editor** — and any NEW upload made from inside the canvas editor is auto-tagged to that same restaurant (via a new `merchant` prop threaded from `fields.restaurant_name` down through `FieldEditor` → `ImageUpload`), so normal editing needs zero extra steps to stay organized by merchant. Stickers and QR codes (already their own folders since 2026-07-21) get the exact same merchant scoping automatically — no separate mechanism needed.
- **Verified locally** via a mocked `/api/library-assets` (local `vite dev` still has no working serverless layer) — merchant filtering, search, the auto-default-to-current-restaurant behavior, and upload prop-threading (confirmed via React fiber inspection, since a naive raw-DOM `select.value` mutation doesn't reliably trigger React state and gave a false-negative on first attempt) all checked out.
- **Fixed same day: casing typos creating duplicate merchant folders.** Julia hit exactly the limitation flagged above ("Wen Cheng" vs "wen cheng"). Fixed properly rather than just patched for this one case: `api/library-assets.js` now has a shared `resolveMerchant()` helper that snaps a requested merchant name to an existing merchant's exact casing when they match case-insensitively (first-seen casing wins) — applied on both upload (POST) and rename/move (PATCH). The Library page's merchant selector does the same snap client-side before even hitting the server. Also added a **"move to merchant" action** — in the "All merchants" view, click an asset's merchant tag to reassign it to any other merchant, so existing stray-cased assets can be consolidated manually.
- **UX fix (2026-07-24): merchant tagging wasn't obvious.** The page-level "Merchant" filter silently deciding what a new upload got tagged with took real figuring-out to notice — no visual link between the two. Replaced with an explicit pop-up on every upload click: **"Add to existing merchant"** (dropdown, defaults to whichever merchant is currently being viewed) or **"Add new merchant"** (name field) — the file picker only opens once you've chosen. `LibraryPage.jsx`'s `MerchantPickerModal`. The top filter is now purely a view filter with no upload-tagging side effect.

---

## Activation key system

Keys gate access to the app. Set in Vercel Environment Variables:

```
WILDCAST_KEYS=WOLT-DE-demo-key|Wolt DE|20,WILD-Demo-KEY|Wild Stack|100|designer
```

Format: `key|Client Name|credits|role` — role is **optional** (added 2026-07-13; omit it and a key behaves exactly as before, defaulting to `partner`) — comma-separated for multiple keys. `role:designer` unlocks the "Import" nav item (self-serve Figma import, see above).

| Key | Client | Credits | Role | Purpose |
|-----|--------|---------|------|---------|
| `WOLT-DE-demo-key` | Wolt DE | 20 | partner | Client demo |
| `WILD-Demo-KEY` | Wild Stack | 100 | *(set to `designer` for Julia + colleague)* | Internal use |

**Also needs `FIGMA_TOKEN`** (separate env var, for the self-serve import feature) — see the "Self-serve Figma import" section above and `ACTIVATION_KEYS.txt` for exact setup steps.

**How credits work (demo):** 1 PDF export = 1 credit. Tracked client-side in localStorage. Credits shown in editor breadcrumb bar. Export blocked at 0. Sign out button in header clears the session.

**Production upgrade path:** Replace localStorage credit tracking with Vercel KV (server-side Redis). Add payment integration (Stripe) → webhook auto-generates key → emails to customer. Half-day build when ready.

---

## Custom domain

- **Target:** `cast.wildstack.studio`
- **Vercel:** Domain added ✅ — waiting for DNS
- **IONOS action required:** Add CNAME record to `wildstack.studio`:
  - Type: CNAME
  - Host: `cast`
  - Points to: `7b09b04663fd7de5.vercel-dns-017.com`
  - TTL: 3600
- Once IONOS record is added: Vercel → Domains → Refresh → goes green automatically

---

## Next steps (priority order)

### 0. Accounts section for seats (planned, not scoped — Julia's note 2026-09-19)
A dedicated Accounts section for managing seats (by Wolt) is planned for later. The building blocks already exist (email accounts, seat cap, personal folders per seat — see the account routes in `api/`); what's missing is a management screen. Natural time to also fix the auth gap on partner-level API routes (see Prompt Brief open items) and to decide how chat/AI credits are counted per seat.

### 1. ⏳ IONOS DNS record (waiting on executive)
Add CNAME for `cast.wildstack.studio` → see Custom domain section above.

### 2. ⏳ Google Drive export (waiting on executive confirmation)
Wolt executive requested Drive export in addition to local download. Blocked on two clarification questions:
- Does she use her @wolt.com work account or personal Gmail for Drive?
- Does her current tool show a Google OAuth popup, or does it connect silently?

Answer determines whether DoorDash/Wolt's Google Workspace security settings will allow a new OAuth app. If yes, ~1 day to build using Google Drive API v3 in-browser OAuth flow.

### 3. Production activation key system (when taking real customers)
- Vercel KV for server-side credit tracking (credits can't be manipulated)
- Stripe payment → webhook → auto-generate key → email to customer
- Admin dashboard to view/manage keys and credit usage

### 4. More templates (on hold — pending client buy-in)
- Flyer Option B–E (different designs, same format)
- Poster format (portrait A3/A2)
- Wild Poster (landscape)
- Retail category templates

### 5. New zone types for the next Figma import — pipeline-side support ✅ done, Julia still needs to design + import the real master
The next template needs three things the original `zone:<id>` convention didn't handle — **all three now supported as of 2026-07-21:**
- **Sticker/badge** (e.g. a circular "26% OFF" badge) — `zone:sticker` now imports as an image zone (contain-fit, requires transparent PNG), matching Julia's instinct that this shouldn't be editable text.
- **QR code** — `zone:qr` now imports as an image zone (contain-fit, square preview, no transparency requirement since QR generators commonly export flat PNGs). **Rename support also shipped** — click an asset's name on the Library page to relabel it, so multiple QR codes don't become indistinguishable.
- **Promo/discount code box** (e.g. "CODE FRESSNAPFMUC10") — already worked with no changes; any `zone:<id>` that isn't `logo`/`photo`/`sticker`/`qr` becomes a text zone automatically (e.g. `zone:code`).

**Update 2026-09-21 — no longer outstanding.** Julia has since designed and imported a real master with these zones: Option C (live) has `logo`, `qr`, `headline`, `sub_headline`, `photo`, `tc` and `sticker`, and shows on production. The Import review page and editor both handle them; the Prompt Brief chat dropping sticker/QR answers was a separate bug, fixed the same day (see the 2026-09-21 entry at the end of this file). A `zone:code` text box has been verified only at the mapping level (unit check), not yet with a real imported template.

---

## Tech stack
- React + Vite, hosted on Vercel (auto-deploy from GitHub)
- Fabric.js v5 canvas editor
- Adobe Fonts — Omnes Pro + Omnes Condensed via Typekit (uot3jfu)
- Template backgrounds: PNG stored in Vercel Blob
- Project saves: JSON stored in Vercel Blob (private), registry in localStorage
- Comments: stored in Vercel Blob at `comments/{id}.json`
- CMYK export: `sharp` (libvips) + bundled FOGRA39 ICC + manual PDF/X-4 builder
- Activation keys: env var `WILDCAST_KEYS`, credits tracked in localStorage (demo); Vercel KV planned for production
- AI copy suggestions: Anthropic Messages API (`claude-sonnet-5`, tool-forced JSON output), key in env var `WILDCAST_COPY` (not `ANTHROPIC_API_KEY` — named that in Vercel before the code existed, code matches it, not the other way round). Grounded in a published-to-web Google Sheet of real historical campaign copy, fetched as CSV via `papaparse`, shared between `api/ai-suggest.js` and `api/presets.js` through `api/_lib/campaignSheet.js`. **Vercel plan: Pro** (upgraded 2026-08-06 — Hobby's 12-serverless-function cap was blocking new API routes from deploying at all; this repo has 14 as of today).

---

*Last updated: 2026-07-13 — Built self-serve Figma import: paste a link into WildCast's new "Import" screen (designer-role-gated), it lands as a draft, publish when ready — no code deploy needed for a new template anymore. Templates stay hybrid: Option A/B still hardcoded (untouched), only new imports go through Vercel Blob into empty catalogue slots. Needs a new `FIGMA_TOKEN` Vercel env var to actually run — see "Self-serve Figma import" section above. Not yet tested against a live deploy (no local serverless emulation available); draft-first design makes that low-risk. Prior entry (same day): built the manual CLI import pipeline (`scripts/import-figma-template.js`) and used it to upgrade Option A + replace Option B.*

*Update 2026-07-20 — Julia reported after live testing that "updates seem not committed." Verified directly (not assumed): everything was committed, pushed, and Vercel had auto-deployed it correctly — confirmed by pulling the live JS bundle and finding the new code strings in it. The real gap was the `role` field on `WILDCAST_KEYS` never being added in Vercel (still needed — see Activation key system), which is why the Import nav looked "missing." Also found and fixed two real bugs from live testing: (1) Option B's catalogue tile was reusing its real designer background as the thumbnail instead of a placeholder-labelled preview like Option A has — generated a matching placeholder PNG from Option B's actual zone coordinates; (2) added a "choose from library" option next to every image zone's upload button, pulling from the existing per-folder asset library (logos/product images) instead of requiring a fresh file picker every time. Both fixed, committed, pushed.*

*Update 2026-07-21 — Julia set up `role:designer` + `FIGMA_TOKEN` in Vercel; confirmed live end-to-end (Import nav shows, submitted a real request to Figma's API and got a genuine response back, proving the token round-trips correctly). Also fixed, same day: Option B's real background color (`#00C2E8`, not Option A's `#00C2CB` — confirmed against the Figma master directly), Option B's CTA weight (real Omnes Pro Semibold/600, confirmed genuinely loaded in the Typekit kit), and a real wrap bug on Option A's `restaurant_name` zone in Designer mode (no manual size control existed for that field in any mode, but the auto-shrink safety net was Guided-mode-only — fixed with a new `alwaysShrink` zone flag). Biggest piece: rebuilt the asset library on Vercel Blob for genuine hi-res, cross-browser storage, with a direct upload UI on the Library page — see "Asset library" section above, including two real Vercel gotchas (function count limit, private-only Blob store) worth reading before adding any new API route.*

*Update 2026-07-21 (cont'd) — Option B's catalogue tile preview (`preview_opt-b.png`) went through a few rounds: first a code-generated placeholder (wrong font — generic Arial instead of Omnes Condensed, since a server-side raster can't legitimately load a Typekit font file), then a corrected version rendered live in-browser onto a canvas element to use the real Typekit font, then found the label sizes were arbitrary guesses instead of matching each zone's real configured `fontSize` (headline was rendering far too small). Julia then just designed and exported the real tile herself directly in Figma at true 4x (1191×1679, matching Option A) — replaced the file, no code change needed since `TemplatePicker.jsx` already points at that same path. **Lesson for next time: ask Julia to design/export catalogue tile previews herself from the start** — she has full control over exact copy/fonts/layout and it's more reliable than trying to reverse-engineer her visual style in code. Confirmed live via direct file hash comparison.
**⚠️ Flagged, not yet resolved:** this tile has real "McDonald's" branding baked into the copy ("...bei McDonald's bestellen"), which conflicts with the 2026-07-10 client requirement (commit `09c34c2`: "Client doesn't want real merchant branding shown in the template picker previews"). Asked Julia twice; she pushed ahead without addressing it either time — re-raise if it comes up again, don't silently assume it's fine.
**Heads up from Julia — next templates need new zone types not yet supported by the import pipeline:** (1) a sticker/badge (e.g. a "26% OFF" circular badge) — Julia's own instinct is image-upload, not an editable text field, given how visually specific these are; (2) a QR code — image upload, plus **needs a rename/label capability** since multiple QR codes will look visually identical and be impossible to tell apart otherwise; (3) a promo/discount **code box** (e.g. "CODE FRESSNAPFMUC10") — a short text field, but a distinct *kind* of field from headline/offer (a discount code, likely wants a distinct visual treatment). None of these map cleanly onto the current `zone:logo`/`zone:photo`/(anything else = text) convention in `api/_lib/figma-import.js` — needs real design work before the next Figma master is built. See reference screenshots Julia shared (AKKO Chicken & Grilled, Wolt×Fressnapf) for what these look like in practice.

*Update 2026-08-03 — New front page shipped: the briefing form. Instead of a 3-step template picker, the homepage now collects a full brief (10 fields, matching a spec Annika drafted from a client working session — see the field spec/pivot memory) and auto-generates 2 filled preview candidates (Option A & B, the only live templates today) to pick from. See the new "Briefing Form" section above for the full shape. Built and deployed same day: candidate generation/picker, a new locked-down "restricted review" editor mode, Library-backed Sticker/QR/Food-photo pickers, and a demo (non-functional API) AI headline suggestion button reusing the existing `AISuggest.jsx`.*

*Update 2026-08-03 (cont'd) — Julia tested live immediately and sent a dense round of feedback, all fixed and deployed same day: real Library pickers for Sticker/QR/Food-photo (replacing a hardcoded sticker list and raw QR upload) plus a new "McD" test partner; "Pick a design" reworked from click-to-open to tick-to-select with two explicit actions — Send for Review (skips editing entirely) and Edit (single-select only); two-column front-page layout recovering the old landing page's hero copy (found still sitting unused in `TemplatePicker.jsx`'s dead `mode="hero"` path).*

*Update 2026-08-03 (cont'd, same day) — Julia tested again and found: the form was jumping to the picker mid-fill (root cause: pressing Enter in a plain text `<input>` natively submits a form when a submit button exists — now blocked, only the Submit button submits), no visible Restaurant-name field (now explicit, defaults to Partner name), and — most importantly — a real font-size bug that an earlier same-day fix hadn't actually solved. **Real root cause, found via temporary console.log instrumentation rather than continued guessing from screenshots:** auto-shrink computes a zone's correct fit-to-box font size and applies it directly to the Fabric canvas object on load, but never writes that number back into React state — so the side panel showed the *static unshrunk default* (e.g. "54pt") while a zone was actually rendering much smaller (e.g. 21pt). Clicking "+" once therefore jumped from the true 21pt straight to 55pt, not a gentle nudge — that dramatic jump is what looked like "headline resizing subline" (the two fields were never actually coupled). Fixed by seeding React state with the real post-shrink sizes (`TemplateCanvas`'s existing `getEffectiveFontSizes()`, already used by `doSave()` for the identical reason) the moment the canvas finishes loading. Verified live, twice, in both directions. Also improved: Sticker/QR/Food-photo empty-library messages now explain they need uploading via the Library page first, instead of an unhelpful "Nothing in the library yet."*

*Update 2026-08-03 (cont'd, same day) — One more feedback pass: Offer now gets the same Scale + Position nudge as Headline/Sub-headline in the restricted editor (was fully locked before). Bigger change: added a real **Save** button to the restricted editor (previously only Send for Review existed there) — persists via the normal save-project/Designs mechanism (so an interrupted session is never lost) and returns to "Pick a design" afterward, so a merchant wanting both Option A and B isn't stuck picking just one. Required lifting the submitted-brief/candidate state out of `BriefingForm`'s local state up into `App.jsx` so it survives the round trip to the editor and back.*

*Update 2026-08-03 (cont'd, same day, Round 5) — Julia's earlier "Subline jumps to templates" report turned out NOT to be stale cache after all: the real cause was `AISuggest.jsx`'s "✦ AI Suggest" button missing `type="button"`, so inside the brief's single `<form>` it defaulted to `type="submit"` — clicking it to open suggestions submitted the whole brief. One-line fix, verified live. Bigger fix: re-clicking Edit on a candidate already saved once via "Save & pick another design" was silently discarding that saved edit and regenerating a fresh, un-edited candidate every time (found by reading the save/reopen code, since local dev has no backend to reproduce it live against — confirmed by mocking `/api/save-project`). Now a saved candidate's font sizes/positions/project id are recorded (`App.jsx`'s new `savedCandidateIds`) and reopened from the session cache on re-Edit, so repeated edits correctly update the same saved project instead of losing work or duplicating it. Also added a "+ New Brief" nav item to start a fresh brief from anywhere. Clarified for Julia: Sticker doesn't work today for two independent reasons — the picker is scoped to one shared library folder, not per-partner, AND neither Option A nor B has a sticker zone to render one on yet.*

*Update 2026-08-04 (Round 6) — Julia retested: Round 5's AI Suggest and saved-edit-persistence fixes both held (no complaints), "+ New Brief" confirmed working. Two things clarified as non-bugs: Subline showing up as "CTA" on Option B is by design (B's background art has the first CTA line baked in, the free-text zone is line 2 only, already filled from Subline); the T&Cs fineprint zone renders real content correctly, it's just 4.5pt and pressed against the left bleed edge, easy to miss without zooming — confirmed by exporting the raw canvas pixels, not just eyeballing it. The real bug: "Edit answers" only resets to blank after a specific round trip — Submit → Pick a design → Edit → Save & pick another design → Edit answers. `BriefingForm`'s local form state was never seeded from the already-submitted brief on remount (it fully unmounts/remounts crossing into and back out of the editor screen), so that specific path lost every typed answer. Fixed with a one-line lazy state initializer. Verified live with the exact multi-hop sequence.*

*Update 2026-08-04 (cont'd, same session) — Julia pushed back on the CTA/Subline explanation and flagged it as "not centred." Verified centering directly against the live Fabric.js canvas object (not a screenshot guess) — it's exactly centered. She confirmed she wants Option B's CTA line to be its own field rather than reusing Subline, so it's now a separate optional "Call to action (Option B only)" field in the brief (falls back to Subline, then the objective text, if left blank).*

*Update 2026-08-06 — Started from an unrelated Wild CMYK (Figma plugin, separate product) audit that surfaced a real backend bug there (wrong ICC rendering intent), then pivoted into a full afternoon of WildCast work at Julia's request. In order:*

*1. **Headline/Subline UI restructure** — replaced the plain input + small AI Suggest pill with a 3-button row per Annika's design spec: "Write my own" / "Choose preset" / "AI Suggest" (Ask agency deliberately left out — no clear implementation path). New `PresetPicker.jsx`, `AISuggest.jsx` restyled via a new `variant` prop (its other usage in `FieldEditor.jsx` untouched).*

*2. **Form paginated into 3 steps** — per Julia's screenshots of where the natural cuts should land (basics / copy / assets+submit). Validation now gates "Next" per-step instead of only at final submit; brief state persists across steps so Back never loses anything typed.*

*3. **AI Suggest and Choose Preset made real** — both were placeholder/mock at the start of the day. Built `api/ai-suggest.js` (real Claude API call, tool-forced structured output) and `api/presets.js` (real historical campaign copy served verbatim, no AI), both reading a published-to-web Google Sheet via shared `api/_lib/campaignSheet.js`. Two real bugs found and fixed from Julia's live testing: suggestions were combining headline+tagline into one long line — fixed by enforcing the same `CHAR_LIMITS` `FieldEditor.jsx` already uses (headline 30 chars, etc.), both in the prompt and as a server-side filter, not just a prompt request; Choose Preset was showing unrelated merchants' copy (e.g. picking McD surfaced a churro campaign) — fixed with `filterRowsByPartner()`, matched against the sheet's free-text "Tasks" column, falling back to the full list when a partner has no library entries yet (most don't, today).*

*4. **Deploy pipeline was broken, unrelated to any of the above code** — Vercel's GitHub auto-deploy had silently stopped triggering (confirmed by diffing the live JS bundle against what was actually on `main` — Vercel was serving a build 2 commits stale with zero indication anything was wrong). Root cause found by disconnecting/reconnecting the Git integration in Vercel Settings, which surfaced the real error on the next push: **"No more than 12 Serverless Functions... on the Hobby plan"** — this repo already had exactly 12, and the day's 2 new API routes pushed it over. Julia upgraded to Vercel Pro, which removed the cap. Separately, the Anthropic key was saved in Vercel under `WILDCAST_COPY`, not `ANTHROPIC_API_KEY` as the code expected — code updated to match rather than having her redo the Vercel setup. Every fix in this update was verified against the real live production endpoints/bundle before being reported done, not assumed from the deploy succeeding.*

*5. **Upload from computer** added to the Sticker/Food photo/QR pickers in the brief (previously library-pick only, by deliberate original design — see the 2026-08-03 entry). Reuses the exact same validation already enforced on the Library page (`LibraryPage.jsx`'s `UPLOADABLE_FOLDERS`, `lib/image.js`'s `hasTransparency`): Sticker and Food photo must be real transparent PNGs, not a flattened image with a solid background; QR codes have no transparency requirement. A successful upload saves to the shared Library and selects it for that field in one step.*

*Update 2026-08-07 (Round 7) — Long session, five parts:*

*1. **AI Suggest now costs credits.** Previously free; now confirm-gated ("Generate 6 AI suggestions? This uses 1 credit") and deducts from the same `activation.credits` pool exports already use. New **"✨ Improve with AI"** button (separate from AI Suggest, next to it) sends whatever the partner already typed as `seed` — the server detects its language and either polishes it or translates it, instead of generating fresh copy from the brief. Also 1 credit. **"More options"** on Choose Preset (free — reveals the rest of what's already fetched) and **"Suggest more"** on AI Suggest (1 more credit, one extra round only, server told what's already shown so it doesn't repeat itself) both added. Char limits tightened 30/60 → 20/25 (headline/subline) to match a shorter house style Julia specified. New copy source sheet (`campaignSheet.js`'s `SHEET_CSV_URL` now points at a different tab, same doc) — verified the new tab's header row matches what the code already expects, zero other changes needed.*

*2. **Real bug found and fixed: stale candidate preview.** `TemplateCandidatePicker`'s card thumbnails were always redrawn from the original brief snapshot, never from what was actually saved in the editor — "Save & pick another design" silently kept showing the pre-edit design. Fixed by threading the freshly-saved preview PNG back through new `savedCandidatePreviews` state. Julia reported "still not fixed" after the first push — rather than assuming the fix was wrong, cross-checked two independent signals (fetched the deployed JS bundle directly and grepped for the new code; had her screenshot the Vercel deployment's commit hash) — both confirmed the fix WAS live, then re-reproduced successfully against production twice right after. Root cause of the "still broken" report was never pinned down for certain (likely CDN propagation timing at the exact test moment), but the fix itself was never actually broken.*

*3. **UX corrections + polish.** Restaurant name hint said "Option B" — checked the actual zone data and corrected it to Option A (the opposite of what Julia guessed). Headline/Subline labels clarified with their real on-canvas order (Subline renders *above* Headline) — confirmed via zone y-coordinates and independently via a live render. Project name now auto-tags from the brief (`Merchant – Offer – Restaurant Flyer · Option A`) instead of the generic template name. Designs page filter popup gets a third "Date saved" dropdown. Clicking Templates/Library/Designs/etc. from an open editor now confirms before leaving (was silently discarding unsaved edits) — logo click deliberately exempt. Option B's CTA font was fake-bold: fetched the live Adobe Fonts/Typekit kit CSS directly and confirmed `omnes-cond` only has weight 400/700 registered, so `fontWeight: 600` on it was browser-synthesized, not real Semibold — switched to `omnes-pro`, which has a genuine 600 face. Help modal was still describing the pre-pivot 3-step template-picker flow with zero mention of AI Suggest/credits — rewritten to match the current flow plus a plain-language breakdown of what each copy tool costs.*

*4. **Real server-side auth added — was missing entirely.** Found while investigating the Figma import architecture (see FIGMA_IMPORT_ROADMAP.md, updated same day): `api/import-figma-template.js` and `api/publish-template.js` were gated by `activation?.role === 'designer'` in the UI only — the routes themselves checked nothing, so anyone with the URL could call them directly with no key at all. New `api/_lib/auth.js` mirrors `validate-key.js`'s `WILDCAST_KEYS` parsing and requires an `X-Activation-Key` header on every request to both routes. Verified the auth logic directly with a Node script exercising both check functions against real/wrong-role/garbage/missing keys — all correct.*

*5. **New `role:'agency'` tier**, split out from `role:'designer'`, at Julia's request — she wanted to test the Import screen herself while keeping it hidden from a client-facing test key. `designer` keeps template-management access (archive/publish/zone review) but not Import; `agency` gets everything `designer` gets plus a fully working Import tab (no "Coming Soon" gate). Enforced on both the frontend (`Header.jsx`/`App.jsx`) and the backend (new `requireAgencyKey`, stricter than `requireDesignerKey`, applied only to the import-creation endpoint). Julia updated `WILDCAST_KEYS` in Vercel herself and confirmed it's live. Also, while investigating the same screen: found the Import review panel only ever let a designer adjust font size/rotation, never position or size, and didn't show image zones (Logo/Photo) at all — so a bad bounding box from Figma had no fix short of redoing the whole import. Added X/Y/Width/Height fields for every zone plus a live visual overlay on the background preview so misalignment is visible immediately. No backend changes needed — `updateZones` already accepted arbitrary zone data.*

*Also this session: the Import nav tab itself was greyed out with a "Coming Soon" popup (`designer` role) while the Figma-plugin rework is pending — see FIGMA_IMPORT_ROADMAP.md for the actual plugin plan, updated same day with what's changed since it was written.*

*Update 2026-08-07 (cont'd, same day) — Julia asked to get the Figma plugin itself moving. First pass of the whole pipeline written: `figma-plugin/` (manifest.json, code.js, ui.html) — reads the selected Figma frame via the Plugin API, exports its background PNG directly (`node.exportAsync`, no Figma REST API, no token), and POSTs raw node data to a new `api/import-figma-plugin.js` endpoint gated by a shared secret (`requirePluginKey`, new in `api/_lib/auth.js` — distinct from the `role:agency` system, since a plugin has no logged-in WildCast user). Key design call made during the build, not in the original plan: the actual zone-geometry/font math runs **server-side** (new `toCanvasZoneFromPluginNode()` in `figma-import.js`, reusing the exact same `boxToZoneRect`/`IMAGE_ZONE_CONFIG`/`ROTATED_TEXT_DEFAULTS` the REST path already uses) rather than porting that math into the plugin's own JS runtime — the plugin just extracts raw node data and exports the PNG. Also: Figma's plugin sandbox has no image-processing library, so the crop-to-trim step (needed either way) stays server-side too — refactored into a shared `cropToTrim()` both import paths now call, instead of duplicating it. Verified directly (not just read-through): a Node script fed realistic mock plugin-node data through the new zone function and confirmed all 12 checks passed (text font extraction, image zone config, the marker-plus-sibling-text fallback, style-name-to-numeric-weight mapping, the no-text rotated-default fallback); `cropToTrim()` verified against a real generated PNG. **None of this has been run against an actual Figma file yet** — that needs Julia's Figma desktop, see FIGMA_IMPORT_ROADMAP.md's "Start here Monday" section for the exact setup + first-test steps, including the one open technical question (whether plugin-sandbox network calls really do bypass CORS as assumed).*

*Update 2026-08-13 — Julia's first real load of the plugin in Figma desktop hit "Could not load slot list: Failed to fetch." Root cause: the CORS open question from the 2026-08-07 entry above resolved the other way — main-thread `fetch()` does NOT bypass browser CORS in Figma's webview, and `api/list-templates.js`/`api/import-figma-plugin.js` had no CORS headers at all. Fixed with `Access-Control-Allow-Origin: *` on both plus explicit `OPTIONS` preflight handling on the POST endpoint (its `x-plugin-key` header triggers one); confirmed live via `curl`. Also: plugin header logo resized (15px → 26px, Julia's ask), and a small banner added to the web Import page pointing designers at the plugin. Then, at Julia's explicit request and ahead of the roadmap's own caution to wait until the plugin's proven end-to-end — she was told the risk (no fallback import method if the plugin's first real test turns up a bug) and asked for it anyway — **Step 5 of FIGMA_IMPORT_ROADMAP.md done**: deleted `api/import-figma-template.js` (the paste-a-URL + Figma personal-access-token web route) entirely, its token-entry UI in `TemplateImportPage.jsx`, and `requireAgencyKey()` (its only caller, in `api/_lib/auth.js`). That page is now review-only — lists draft/live records the plugin created (`customRecords` filtered to real imports, excluding the synthetic Option A/B override records) and reuses the same zone-overlay/position-size-font editor/publish panel, now populated by picking a record from a dropdown instead of only right after a same-page import. Also removed the page's separate "Previously imported" publish/archive list — confirmed first that `TemplatePicker.jsx`'s own per-card manage menu already covers Publish/Unpublish/Archive/Restore, so nothing was lost. **Kept** `figmaFetch()`/`parseFigmaUrl()`/`importFigmaTemplate()`/`toCanvasZone()` in `api/_lib/figma-import.js`, deviating from the original roadmap plan to delete those too — `scripts/import-figma-template.js`, a local CLI script still actively recommended as a fallback (see `project_template_rules` memory), depends on all four and would have broken for no reason connected to what was actually asked. **The plugin still hasn't completed a real end-to-end import** — that's the next real test, now with no fallback.*

*Update 2026-08-13 (cont'd, same day) — the real end-to-end test happened, in two more rounds. First: after the CORS fix, the plugin's Import button hit "A valid plugin key is required for this endpoint" — turned out `FIGMA_PLUGIN_KEY` had never actually been added to Vercel, despite being step 1 of the roadmap's own "Start here Monday" checklist from 2026-08-07/08. Gave Julia the exact value (matching the `PLUGIN_KEY` constant in `figma-plugin/code.js`) to paste into Vercel + redeploy. Second, after that: **a real import succeeded** — Julia confirmed "I tested it now, and it works," landing `restaurant-flyer-option-e` as a genuine plugin-created draft. Her follow-up testing in that same message surfaced four more real issues, all fixed and deployed same session:*

*1. **Archive didn't actually free a slot for reimport** — real bug in `figma-plugin/code.js`'s `loadEmptySlots()`: it excluded ANY slotKey with an existing record at all (draft or archived, not just live), contradicting `api/publish-template.js`'s own documented behavior ("archive → slot freed up for a fresh import"). Fixed to only exclude live slots.*

*2. **No working delete existed.** `api/delete-template.js` had been sitting in the repo since ~2026-08-07, fully unauthenticated and wired to no UI at all — found while investigating Julia's "archive doesn't work" report. Added `requireDesignerKey` and a "Delete permanently" option (red, in the existing ⋯ manage menu) for archived/draft records in `TemplatePicker.jsx`, gated behind a confirm dialog. Used it immediately to clear two stale archived test records (`restaurant-flyer-option-c`, `restaurant-poster-option-c`) at Julia's request.*

*3. **Drafts were clickable in the main Templates catalogue, opening the real partner-facing editor** — a deliberate feature from the 2026-08-07 session (see that entry above: "the actual 'preview a draft' feature"), but now redundant and confusing alongside the Import page's own real review UI — Julia: "it shouldn't just go to Slot E, should actually first allow us to edit, right?" and "I don't want it to appear in the templates before it is actually published." Removed the click-to-open behavior in `OptionsView` (`TemplatePicker.jsx`); the Draft badge and manage menu stay, reviewing a draft's zones now only happens on the Import page.*

*4. **Zone settings panel was "very cramped."** Root cause: the X/Y/W/H/Font-size number inputs were fixed at 48-52px wide, clipping their own decimal values ("44.1" rendering as "44,"). What read as "the T&C font size is way too big" was very likely this same clipping, not an actual math bug — `tc`'s fallback size (18) was already the smallest value on the panel. Redesigned `TemplateImportPage.jsx`'s review screen into a two-column layout (sticky preview left, labeled zone-setting cards right) explicitly matching the app's own real editor's canvas-left/panel-right pattern — Julia pointed at that as the reference to follow — with full-width grid-based inputs that show complete values.*

*All four verified and deployed (commit `bb223e8`) before Julia went to test another round herself. Repo-doc updates (this file, `FIGMA_IMPORT_ROADMAP.md`) and Claude's own cross-session memory both updated same day to reflect current state — see `project_figma_import_plugin` memory for the full narrative if picking this up in a future session.*

*Update 2026-09-08 (Round 8) — Client-requested workflow reversal: the 2026-08-03 pivot (3-step wizard → AI generates candidate mockups to pick from) is replaced with a single-screen brief → template picker → "Choose your mode" → live editor flow, much closer to the original pre-pivot template-first experience. `BriefingForm.jsx` trimmed from 3 steps to 1 (just enough to route to a template); new `BriefTemplatePicker` in `TemplatePicker.jsx` reuses the existing catalogue page's `OptionsView`/`LayoutModal` machinery rather than building new UI, auto-scoped to the brief's Business type + Formats. The editor pre-fills from the brief (partner/restaurant name, offer text, auto-pulled logo/photo) using the same `buildCandidateFields()`/`fetchMerchantAssets()` the old flow already had. `TemplateCandidatePicker.jsx`/`TemplatePreviewModal.jsx` and the restricted-review editor mode are kept in the codebase, deliberately unwired rather than deleted, in case they're wanted again. Local `main` was 2 commits behind Anang's `feature/anang` merge at commit time — fast-forwarded and reconciled via `git stash`, no work lost either side. Commit `e77a200`.*

*Update 2026-09-08 (Round 9, same day) — Julia's first live test of the new flow surfaced one real bug and two non-bugs. **Real bug:** typing a long headline/offer into the guided-mode editor could make the size stepper jump wildly on the first click — auto-shrink was updating the live canvas on every keystroke but never syncing the new size back into React state (same bug class as an earlier 2026-08-03 fix, just triggered by continuous typing this time instead of once at mount). Fixed with a new `onAutoShrink` callback on `TemplateCanvas`. Also shipped: Position nudge on the Offer field (guided mode's canvas is locked, same as restricted mode already had), T&Cs textarea now wraps long unbroken text (missing `overflow-wrap`), and the brief's separate "Restaurant name" field was removed (always uses Partner name now, matching the existing fallback in `buildCandidateFields`). **Confirmed NOT bugs, verified live rather than assumed:** AI Suggest and the image-zone "choose from library" link both looked broken locally but are unchanged, pre-existing code hitting endpoints (`/api/ai-suggest`, `/api/library-assets`) with no local-dev equivalent — the same long-standing limitation as ever, not a regression from this rework. Commit `14e5098`.*

*Update 2026-09-08 (Rounds 10-12, same day) — Working through a self-authored fix-priority checklist Julia sent as a standalone HTML artifact (kept at `~/Downloads/flyer-tool-checklist.html`, outside this repo — not committed). Shipped, in order: a one-line explainer above Business type ("these answers help pick the right template"); "Sticker" renamed to "Discount" everywhere a partner actually sees it (Library page's folder filter + upload button, plus a forward-looking display label for FieldEditor's per-zone rendering) — the internal folder key/zone-id matching stays `stickers` in code, per Julia's explicit ask; confirmed the "Restaurant name is Option A only" ambiguity is already structurally impossible now that each template's editor only ever shows that template's own zones (A and B fields are never shown side by side); Send for Review now confirms before sending; Export PDF states its cost; a "Need more layouts?" popup after Save/Export/Send for Review offers any other format checked in the brief, jumping straight to a picker scoped to just that one format (new `formatOverride` prop on `BriefTemplatePicker`, shared `FORMAT_TEMPLATE_GROUP` mapping moved into `briefConstants.js`); "Formats needed" checkboxes grey out/disable (real `disabled` attribute, not just styling) for any format with no live template yet. Commits `df3ea45`, `8e19c36`, `c52e48f`.*

*⚠️ As of end of day 2026-09-08, Julia stepped away before manually testing any of Rounds 9-12's fixes herself — everything above is "built + locally verified with mocked API calls," not yet confirmed against real behavior on a real deploy. Still open on her checklist: i4 (QR/T&Cs "when needed" guidance — N/A today, no live QR zone or Wild Poster to guide about), i5 (explain Option A vs B earlier in the flow), i8 (photo assignment logic — needs Julia's input on the underlying behavior before writing copy for it), i9 (food photo/CTA text overlap bug — flagged as highest technical priority, not started), i13 (routing the short form into the existing non-designer editor — likely already satisfied by Round 8's rework, needs her confirmation rather than more code).*

*Update 2026-09-09 (Round 13) — Fix checklist moved off the local HTML file onto a **shared, live-updating Claude Artifact** (`https://claude.ai/code/artifact/1074a61c-53ac-4767-af4d-70aaf882d7d0`, "WildCast Fix Checklist") — checking a box there pushes a new version to everyone with the link, same 24-item list carried forward. Julia sent 5 action items plus a screenshot of feedback from Annika Brandt, all shipped same session: the "Pick your template first" button restored (first wired to the full Templates catalogue page, corrected once Julia clarified she meant the original in-form popup — `TemplatePreviewModal`, existed pre-2026-09-08, was unwired not deleted); Annika's follow-up-field red-asterisk + "same as Partner name" checkbox; both template-picker thumbnails replaced with Julia's own real master Figma design files (not code-generated); Offer field starts blank with a greyed example instead of pre-filling the objective label; Option A's food photo can now overlap in front of the headline ("Wolt style") via the existing `overlapAbove` z-order mechanism. Commits `c49ca81`, `bf63eda`, `54ec68d`.*

*Update 2026-09-09 (Round 13, cont'd) — A follow-up batch of 5 more items, all investigated live rather than guessed at:*

*1. **AI Suggest dropdown rendering invisible/off-screen** — also the real cause of a separately-reported "Subline AI Suggestion does not work at all" (it was generating suggestions the whole time, just clipped out of view). Root cause: the dropdown anchored to whichever button (AI Suggest vs Improve with AI) triggered it, not the shared row — on the 360px editor panel a 300px-wide dropdown could start left of the panel's own `overflow:hidden` edge and get clipped. Fixed by anchoring to the full-width button row instead.*

*2. **Wolt/Omnes Cond fields (Headline, Sub-headline, Offer, Restaurant name) now force-uppercase live** — brief-generated text was already uppercased, typing directly in the editor wasn't. Driven off the zone's real `fontFamily` (`omnes-cond`), not a hardcoded field list, so any future Omnes Cond zone picks it up automatically.*

*3. **Food photo no longer auto-pulls from the Library** on brief submit — always starts blank now so a partner explicitly picks the dish per design. Logo still auto-pulls (one fixed brand asset per merchant).*

*4. **T&Cs "still not wrapping" — investigated, could NOT reproduce.** Typed a realistic T&Cs sentence (including deliberately past the 120-char limit) and pulled the actual rendered canvas pixel-for-pixel — confirmed `api/export-cmyk.js` has no separate text-layout code of its own (it just converts an already-rendered PNG, so the exported PDF uses this same canvas), then zoomed the decoded image: wraps correctly into multiple legible lines both times. Left open on the checklist pending a fresh screenshot — may be the same "too tiny at 4.5pt to read without zooming" non-bug investigated back in Round 6, or something this test didn't catch.*

*5. **Option B's Offer/CTA confusion, and a real prefill bug found along the way** — Option B's `cta` zone was falling back to the raw objective label when no CTA/subline was typed, rendering "Jetzt Wolt App downloaden und **New Dish**" — the same "objective label leaks into a content zone" bug class as the Offer fix above, just found on a different field/template. Fixed by dropping that fallback. Also added template-aware field hints (`FieldEditor.jsx`'s `StepFieldRow` gained a `hint` override, mirroring the existing `placeholder` override) — Option B's Headline hint now explains it completes the fixed "Wie wär's mit ..." line as a question, and the CTA hint explains it completes "Jetzt Wolt App downloaden und ..." as one sentence, instead of both options sharing Option A's mismatched generic examples.*

*Commits `bc44c21`, `0f8f2d8`. Checklist now 18/24 dev-done, 0/24 manually tested by Julia — i23 (T&Cs) is deliberately left unchecked pending her confirmation, everything else in this update is checked off with an explanatory note.*

*Update 2026-09-10 (Round 16) — food photo Position nudge, two passes. Julia: Option B's food photo felt "very restricted" — could nudge left/right but not up/down for a photo whose cover-fit scale happened to bind tight on height. Same underlying mechanism as the 2026-07-24 logo-nudge fix (`clampOffset()`'s slack math) and the same-day "stops after ~3 attempts" note below it — but this time the zone's own crop margin (`NUDGE_MARGIN`, previously a flat 1.06) was the actual bottleneck, not just missing feedback. First pass (`c872b55`): bumped the margin to 1.15 and made the clamp `overlapAbove`-aware (a zone with `overlapAbove` can validly nudge further up than plain cover-fit slack allows, since its clip region already extends that far — previously unreachable in practice). Deployed, confirmed live (Vercel build `success`) — Julia retested and reported it **still** didn't work.*

*Investigated rather than re-guessing the percentage again: wrote a standalone simulation of the exact `clampOffset()`/scale formula (not a re-derivation — the literal same math) against Option A/B's real zone dimensions across 8 photo aspect ratios (4:3 through ultra-wide panoramic and portrait). Finding: a flat percentage margin is fundamentally fragile — the tight (binding) dimension's slack depends ONLY on the margin, and for a photo whose aspect closely matches the zone's own, DOWN-direction room plateaued at exactly 3 clicks (12px) regardless of how extreme the photo's aspect got beyond that point. Not zero, but small enough to plausibly read as "doesn't work," especially at 100% canvas zoom where 12px of on-screen movement is subtle.*

*Second pass: replaced the flat `NUDGE_MARGIN` with a per-upload computed margin that guarantees an ABSOLUTE minimum slack (24 canvas units, ~6 clicks) in whichever dimension cover-fit binds on for that specific photo+zone combination, instead of a fixed percentage. Re-ran the same simulation: worst case across all 8 aspect ratios and both templates is now 4-6 clicks (was 3) in every direction, roughly double the old floor, and it's now a deterministic guarantee rather than an accidental byproduct of whatever percentage happened to be set. **Verified via the standalone math simulation, not a live browser click-through** — this tool session has no way to drive a real file-upload dialog, so the actual on-screen Fabric.js rendering hasn't been re-confirmed by hand; asked Julia to retest.*

*Julia retested — confirmed "absolutely perfect." Round 16 closed.*

*Update 2026-09-10 (cont'd) — Julia asked what the "What's the offer?" follow-up question + "Use same name as Partner name" checkbox under Objective actually does, since it was confusing (shows for every objective with a follow-up, but "use partner name" makes no sense for e.g. Special offer). Traced `brief.objectiveFollowUp` through the whole pipeline: `buildCandidateFields()` in `briefToCandidates.js` never reads it — the real `offer` field on the flyer is always left blank at submission regardless (a separate, deliberate 2026-09-09 decision, so the partner fills it fresh in the editor). So the follow-up question was required to submit the form but had zero downstream effect — pure friction. Asked Julia what to do (remove it / wire it into the real field / keep but fix the checkbox); she chose **remove it entirely**. Deleted the follow-up question + checkbox from `BriefingForm.jsx`, the now-unused `followUp` property from every `OBJECTIVES` entry and `objectiveFollowUp`/`followUpSameAsPartner` from `DEFAULT_BRIEF` (`briefConstants.js`). Clean removal — grepped for every reference first, confirmed nothing else read either field. Commit `09bf95e`.*

*Update 2026-09-10 (cont'd) — Julia asked about a QR code input she remembered from "Option B" — turned out to be Restaurant Flyer · Option C, not B. Checking it surfaced a real mistake: earlier this same session, `restaurant-flyer-option-c` was deleted (along with `restaurant-poster-option-c`) on the reasoning "already archived, looks like test debris," without actually reading its contents first — see FIGMA_IMPORT_ROADMAP.md-adjacent work above. That record had the only real `qr` zone anywhere in the system. `api/delete-template.js`'s delete is a real, permanent Vercel Blob removal — no undo. The record's `figmaUrl` pointed at a real, still-live node in Julia's own Figma master file though, so the source art itself was never touched — recoverable by re-running the plugin on that frame into a fresh slot, whenever that happens.*

*While investigating, confirmed (not assumed) that the editor needs zero code changes to support a QR zone whenever one exists again: `FieldEditor.jsx`'s generic image-zone loop already special-cases `zone.id === 'qr'` (square crop, auto-crops the white quiet-zone margin QR exports carry), `assetLibrary.js` already routes it to a `qr-codes` folder, and neither `TemplateCanvas.jsx` nor `export-cmyk.js` hardcode zone names anywhere in their image handling — all built alongside the original Template C import back in July and never exercised since. **Paused on purpose** — Julia: leave the re-import for a later session, once the client signs off.*

*Update 2026-09-11 — Template C got re-imported (Julia's own call, ahead of the "wait for sign-off" plan above) and she hit two real UI problems reviewing its 7 zones on `TemplateImportPage.jsx`: (1) scrolling down to reach a zone further down the list (e.g. sub_headline) scrolled the design preview out of view too - not just "too much content," a genuine CSS bug. The grid's `alignItems:'start'` left the sticky preview column's own box only as tall as its collapsed content (no room to actually stick), and separately this screen had no bounded viewport height at all - unlike `screen === 'editor'`, which already gets `height:100vh, overflow:hidden` specifically so it can have its own internal scroll region instead of the whole page scrolling. Extended that same treatment to `screen === 'import'`. Also moved `position:sticky` off the grid item itself onto a plain wrapper div inside it - even once correctly sized, a grid item that's ALSO the sticky element ran into real containing-block ambiguity in testing; the wrapper pattern is the standard, reliable fix. Verified live (not assumed) by expanding all 7 zones and scrolling top to bottom - preview stayed pinned the whole way. (2) X/Y/W/H felt confusing as raw number boxes. Redesigned each zone into a closed-by-default accordion (needs-review zones auto-expand) so the list stays short enough that reaching any zone rarely needs much scrolling to begin with, and added a slider next to every number input for quick visual adjustment alongside exact entry. Commit `edae99a`.*

*Update 2026-09-11 (cont'd) — two more real fixes from the same testing round. (1) **Publish needed 2-3 clicks to actually show as published in Templates.** Real bug, not just the documented Vercel Blob list()-lag: `onOptimisticPatch()` already applies the action's own known-fresh result directly to shared state, but every handler ALSO called `onRefetch()` immediately after - that fetch could resolve with the STALE pre-action record and, since `refetchCustomTemplates()` fully replaces state rather than merging, clobbered the correct optimistic update right back to "unpublished." One handler's own comment in `TemplatePicker.jsx` already explained this exact failure mode with the bug still sitting on the very next line - found by actually reading the code that close, not just re-testing. Removed the four now-unnecessary immediate `onRefetch()` calls (`TemplateImportPage.jsx`'s publish + save-zones, `TemplatePicker.jsx`'s manage-action + delete) and cleaned up the `onRefetch` prop end-to-end (App.jsx → TemplatePicker/BriefTemplatePicker/OptionsView → TemplateImportPage) once nothing called it anymore, rather than leaving dead plumbing behind. (2) **Zone labels added to the real editor canvas, not just the Import review screen.** Julia's client, in a separate meeting: editing a template needs more visual guidance on what each zone is. Every zone (text and image) on `TemplateCanvas.jsx` now gets the same colored, labeled guide box `TemplateImportPage.jsx`'s `ZoneOverlay` already used on the review screen - blue for image zones, pink for text - confirmed this needs zero new template data (just each zone's existing id/position/type) so it's live on every current template already, Option A/B/C included. Also gave rotated zones (e.g. `tc`) a guide box for the first time - previously only non-rotated text and image zones got one at all. Commit `a8f6982`.*

*Update 2026-09-11 (cont'd) — Julia: after running the plugin, the Import review page still said "No imports yet" until she manually reloaded the whole page. Real gap, not a bug in the fetch logic: the plugin writes straight to the backend from inside Figma, a completely separate process the browser tab has no way to know about - `customRecords` only ever got fetched once, on page load. Reintroduced `onRefetch` (removed end-to-end in the previous update once nothing called it) scoped specifically to `TemplateImportPage.jsx` this time, wired to two triggers: the tab regaining focus (`visibilitychange` + `window focus` listeners - matches the actual workflow of switching to Figma and back) and a small manual "↻ Refresh" button next to the dropdown for anything that doesn't fire a focus event. `refetchCustomTemplates()` in `App.jsx` now returns its own fetch promise instead of being fire-and-forget, so the button's loading state can actually wait for it. Commit `912b373`.*

*Update 2026-09-11 (cont'd) — Julia asked about her Figma workflow (should the placeholder "HEADLINE" text be deleted or just hidden before importing? answer: hidden, not deleted - the plugin reads font info from a layer's own properties regardless of visibility, only the exported background PNG is affected by visibility), then reported the imported headline rendered in "normal Wolt font and not Cond." Checked the actual saved record rather than guessing: Figma really does export the family as `"Omnes Cond"` (Title Case, space) - this app's real registered `@font-face` is `'omnes-cond'` (lowercase, hyphenated, `src/index.css`). Two different strings, so the browser could never find a match and silently fell back to its own default font - `fontSize`/`fontWeight`/position were always extracted correctly, only the family name was wrong, and this has been true for every Figma import since the REST path first shipped, not just Option C. Also cleared up what she called a "font toggle" she'd noticed but didn't understand - it's the L/C/R text-alignment control, unrelated to font family at all; there's no font picker anywhere in the editor today. Added `normalizeFontFamily()` in `api/_lib/figma-import.js`, applied at all four places a zone's `fontFamily` comes from Figma data (REST + plugin paths, main node + sibling-text fallback) - same normalization idea as the existing `weightFromStyleName()`, matches on a loosely-cleaned name ("cond" → `omnes-cond`, "omnes" alone → `omnes-pro`) rather than an exact string, and passes through anything that isn't an Omnes face unchanged rather than guessing wrong. Verified directly (Node script exercising the real function, not just read-through) against "Omnes Cond", "Omnes Pro", "Omnes", an already-correct lowercase name, an unrelated font, and `null` - all four/five cases resolved correctly. Commit `d93aba4`. Takes effect on the next re-import (the same one needed anyway to fix the hidden-layer issue) - no need to touch the already-saved draft.*

*Update 2026-09-11 (cont'd) — Julia asked us to only use real WOLT fonts, not Omnes, and pointed at the WOLT `.otf` files sitting in the repo root's `fonts/` folder (outside `wildcast-app`). Turned out this migration had already happened - `index.css`'s own comment says Adobe Fonts/Typekit's Omnes embed was already removed and self-hosted WOLT files copied into `public/fonts/wolt/` - but only 3 of the 7 files ever got an actual `@font-face` rule: `WOLTCondBlack` (900), `WOLTCondMedium` (500), `WOLTRegular` (400), and `WOLTBlack` (900) were deployed and sitting unused. `TemplateCanvas.jsx`'s `loadFonts()` even had a comment already flagging part of this gap without it ever getting finished. Every Figma-imported headline/sub_headline asks for weight 900 (confirmed against real saved records) - with no matching `@font-face`, the browser was silently substituting the nearest registered weight (700) instead of the real Black file. Added the 4 missing `@font-face` rules and updated `loadFonts()`'s preload list to match. Verified live in the browser (not assumed): all 7 weight/family combinations report `document.fonts.check() === true`, and network requests confirm every one of the 7 WOLT files actually gets fetched, including the 4 previously-orphaned ones. Deliberately kept the `omnes-cond`/`omnes-pro` family names as-is per the existing documented decision - they're internal labels pointing at real WOLT files now, renaming would touch every zone config for no functional gain. Commit `0b846a4`.*

*Also answered (not a separate bug, same root cause): Julia's report that "merchant loves wolt"-style text wasn't auto-capitalizing on the editor canvas despite being in a Cond-font zone. `App.jsx`'s `handleFieldChange` already auto-uppercases any zone whose `fontFamily === 'omnes-cond'` exactly - generic, keyed off the zone's real font family, not a hardcoded field list, so it already covers `restaurant_name` alongside headline/sub_headline. Before the font-family-normalization fix above, a Figma-imported zone's `fontFamily` was `"Omnes Cond"` (capitalized), which fails that strict equality check silently - the exact same string mismatch that broke font rendering also broke auto-caps for anything typed into that zone. Should self-resolve on the next re-import alongside the font fix; flagged to Julia to report back if it's still happening after that, since it can't be fully confirmed without her retesting live.*

*Update 2026-09-11 (cont'd) — Julia exported a real test PDF of the re-imported Option C and reported five things at once: guide/placeholder text and dashed guide boxes baked into the actual export despite being hidden in Figma, tripled "HELLO HELLO HELLO"/"TEST TEST TEST" text (with earlier copies fading, i.e. ghosted), bleed looking misaligned vs. Option A/B on the same A6 canvas size, T&Cs fine print too large, and the QR code appearing doubled. Fixed the one that was a real, confirmed app bug and investigated the rest live before concluding anything: (1) **T&Cs fallback fontSize was `18`, not the app-wide-convention `4.5`** confirmed against Option A's real `tc` zone in `templateZones.js` - `ROTATED_TEXT_DEFAULTS.tc.fontSize` in `api/_lib/figma-import.js` fixed to `4.5`. (2) **Food photo z-order was non-deterministic** - `fabric.Image.fromURL()`'s async callback meant photo-vs-other-image load order (and therefore stacking order) depended on network timing, not code. Julia's ask ("food item should always be the first layer") made this a real bug, not a preference - added an explicit `canvas.sendToBack()` call right after the photo image is added in `TemplateCanvas.jsx`, so it's deterministic regardless of load order. Both verified via code reading against Option A's real config and the exact async callback in question; commit `2140577`.*

*For the other three (guide leak, bleed, doubled QR), reproduced the actual export pipeline live in the browser rather than guessing from code: intercepted the real `fetch()` call `getPng()` makes to `/api/export-cmyk` on Option A (the one template both Julia and this investigation treat as the known-good baseline), captured the exact base64 PNG payload the app sends to the export endpoint, and rendered it back out to inspect directly. Typed real content into the Headline field first (matching Julia's test, which had real typed text) and re-captured. Result: **zero guide leakage, zero duplication, zero misalignment** - `getPng()`'s hide-before-capture / restore-after logic is confirmed working correctly on the general pipeline, for both an empty and a filled canvas. This rules out an app-side bug in the shared export code path itself (the same code every template's export goes through, per Julia's own "same logic option a and b is the same logic option c needs" framing).*

*That narrows all three remaining symptoms to Option C's specific Figma source data / import, not WildCast's code - the actual `restaurant-flyer-option-c` Blob record no longer exists (deleted earlier this session, see the 2026-09-10 entry above) so it can't be inspected directly; this is inference from a clean-baseline test, not a confirmed root cause. Most likely explanations, worth checking directly in the Figma file before re-importing again: (a) the guide/placeholder Figma layers may not actually be hidden on the exact frame selected when the plugin's `frame.exportAsync()` runs (`figma-plugin/code.js`) - that call bakes whatever is visible at that moment directly into the background PNG's pixels, so a layer hidden in a different view/version of the file, or a parent group that's still visible, would still get baked in even though it "looks hidden" when browsing; (b) the ghosted/tripled text strongly suggests multiple overlapping duplicate frames or layers stacked in the same selection (e.g. several draft iterations left on top of each other) rather than a rendering bug, since a single clean export wouldn't produce fading duplicates; (c) the bleed crop (`cropToTrim()` in `api/_lib/figma-import.js`) removes a fixed, symmetric 3mm margin from all four edges assuming the frame's bleed is set up symmetrically - if Option C's frame has uneven padding around its content vs. its true bleed edge (unlike Option A/B, which are already confirmed correct), that would misalign the crop without needing any code change to explain it. Flagged to Julia to check these three things in Figma directly before the next re-import, per her own "ill check" - no code changes made pending that.*

*Update 2026-09-11 (cont'd) — Julia checked the Figma file directly (queried live via the Figma MCP against the real file URL, not just her screenshot): every guide/placeholder layer (`headline`, `sub_headline`, `IMAGE`, all `zone:*` markers, `QR Code`) was already correctly hidden, matching Option A/B's own convention exactly as she said. The one genuine miss was `GUIDE — trim line (105x148mm)` - still visible, confirmed by rendering the frame the same way `frame.exportAsync()` would and seeing the dashed pink line baked into the result. She hid it and re-imported.*

*The fresh import still showed "SUBLINE"/"HEADLINE"/"IMAGE" baked into the background on the review screen. Re-queried the exact same Figma frame live at that point - `headline`/`sub_headline`/`IMAGE` were STILL hidden in the file, contradicting what was baked into the just-imported PNG. Julia pushed back hard that she was on the right frame and the bug had to be in the code - **she was right**. Found it in `api/list-templates.js`'s `?url=` background-image proxy: (1) the upstream Blob fetch had no cache-busting query param, unlike the `.json` record fetch just below it in the same file, which already explicitly cache-busts for exactly this reason (documented Cloudflare-CDN-in-front-of-Blob staleness, same bug class fixed multiple times elsewhere in this file/save-project/publish-template); (2) worse, the response told the BROWSER to cache it for 24 hours (`max-age=86400`). Since a template's `backgroundUrl` is a fixed, deterministic path (`addRandomSuffix: false` - same URL on every re-import of the same slot), the browser's own cache could keep serving the pre-re-import image for up to a day, completely independent of whether the new Figma export or the freshly-uploaded Blob bytes were already clean - exactly what happened here. Fixed both: cache-bust the upstream fetch the same way the record fetch does, and changed the response's own header to `no-store, no-cache, must-revalidate, private` + `Pragma: no-cache`. Commit `21e3eeb`. **Also flagged, not yet fixed**: `api/library-assets.js`'s equivalent asset proxy has the identical pattern (`max-age=86400`, no cache-busting) and library uploads also use `addRandomSuffix: false` - same staleness risk on logo/photo/QR re-uploads, spun off as a separate task rather than expanding this fix's scope.*

*Update 2026-09-11 (cont'd) — after that fix, Julia re-imported Option C again and reported the review page "wants to import the last file not a new one." Second real bug in the same feature, same day: `TemplateImportPage.jsx`'s `result` was a one-time snapshot taken when a record was picked from the dropdown (`setResult(record)`), not a value derived from `customRecords`. The existing auto-refetch-on-focus (2026-09-11, earlier entry) correctly updated `customRecords` after a re-import, and the `<select>`'s option list reflected it fine - but its `onChange` only fires on a genuine user pick, and re-selecting the already-selected option is a no-op in the DOM. So the stale `result` snapshot just sat there showing the old zones/background indefinitely, with no obvious way for Julia to force a refresh short of picking a different option and back (which may not even re-fire `onChange` either). Fixed by making `result` a plain derived value (`reviewable.find(r => r.slotKey === selectedSlotKey)`) computed fresh every render instead of separately-tracked state - a refetch alone now updates what's shown, no dropdown interaction needed. Also removed the now-redundant local `setResult(...)` mutations in `handleSaveZones`/`handlePublish`, since `onOptimisticPatch` already updates the `customRecords` state this now derives from - one source of truth instead of two that could drift apart. Commit `532d9f1`.*

*Update 2026-09-11 (cont'd) — same session, two more asks once the stale-import investigation wrapped up. (1) **"Test draft" button.** The Import review page only ever had "Save zone settings"/"Publish" - no way to actually click around a draft before deciding it's ready, since the earlier (2026-08-13) removal of catalogue-draft-click-to-preview left nothing in its place. Added a third button that opens the currently-reviewed record straight in the real Designer-mode editor via `App.jsx`'s existing `handleSelectTemplate()` - not a separate preview renderer, the exact same code path a live template uses, since `customTemplates.zonesById` already carries every draft's real zone geometry regardless of `live` status. Verified live: mocked a draft record, clicked Test draft, confirmed the real editor opened with the correct zone/font data loaded, no console errors beyond the expected no-backend 404s. (2) **Unsaved zone-edit warning on navigating away.** Julia: leaving the Import page with staged-but-unsaved zone edits silently dropped them, same class of gap `handleNavigate()` already guards against for the editor's own unsaved changes (2026-08-07). Added a matching `importDirty` state in `App.jsx`, reported up from `TemplateImportPage.jsx` via a new `onDirtyChange` prop whenever `zoneEdits` is non-empty, checked by the same `handleNavigate()` guard plus the new `handleTestDraft()` (also a form of leaving the page) - both show the same confirm dialog the editor already uses. Also added a `beforeunload` listener scoped to non-empty `zoneEdits` for actual browser navigation/tab-close/reload, which in-app nav clicks never trigger. Verified live: staged an X-position edit, clicked a nav tab, confirmed `window.confirm` fired with the right message and blocking it correctly kept the edit in place and stayed on the Import page.*

*Update 2026-09-11 (cont'd) — Julia asked to confirm/clarify the food-photo z-order call from earlier today: her exact words this time were "is food image always the top layer?" - the opposite of the `sendToBack` behavior built a few hours earlier off her original "should always be the first layer" phrasing. Asked directly via a structured question rather than guessing which reading she meant - confirmed: photo on top of everything (logo/qr/sticker/text), reversing the earlier fix. Restructured `TemplateCanvas.jsx`'s z-order block: photo is now `bringToFront`'d LAST, after guides/text/overlap-image reordering, then the WildCast-only editing guides are brought to front again right after so zone boundaries stay visible for editing even with the photo now covering real content. Commit `07ba4ea`. Verified live on the real production site (not mocked) - typed a real headline into Option C, uploaded a synthetic test photo through the actual "Click to upload" flow (file-input monkeypatch technique, PNG needs genuine alpha transparency to pass this zone's `requireTransparent` check), confirmed the photo visibly covered the headline text underneath it.*

*Same investigation surfaced two things Julia flagged as still-broken that turned out not to be code bugs: (1) she reported a zone-setting edit on the Import page "didn't save" - checked the live record directly via the API and the edit (a photo zone resize, W 228→278, H 166→173) had genuinely persisted; the Import review screen's preview only ever shows the background PNG + a thin dashed outline box, never an actual composited photo, so a size-only change there has no dramatic visual confirmation, easy to mistake for "nothing happened." (2) she reported the photo-on-top fix still not working even in a fresh incognito window (ruling out the day's earlier browser-cache issues) - live-tested it directly on production myself and it DID work correctly for photo-over-text; asked her for a specific screenshot to pin down whether the remaining gap is actually photo-vs-other-image-zone (logo/sticker/qr), which this particular test didn't exercise, rather than re-guessing further. Not yet resolved as of this entry - pending her follow-up screenshot.*

*Update 2026-09-11 (cont'd) — after a computer restart and switching to her personal browser (not work), Julia confirmed the photo-on-top fix and the Import-page fixes are all working correctly. Two follow-ups from the same message: (1) T&Cs "not wrapping" - turned out to be her own test string (a single 78-character word with zero spaces, no real sentence) which Fabric's Textbox genuinely can't break without a space to wrap on; her actual T&Cs copy (normal sentences) wraps correctly, confirmed live. Not a bug. (2) A real feature request: instead of asking Claude to hardcode a specific z-order every time (today alone it flipped twice), let Figma's own layer order in the Layers panel drive it directly.*

***Zone z-order now derives from Figma layer order** (commit `1a78720`). Every `zone:<id>` marker gets tagged with its position in Figma's own document order at import time - `figma-plugin/code.js`'s `collectAllNodes()` walk (already existed for zone extraction) now tags each node with its index as it's visited (`_zIndex`), which for the flat sibling structure every real template uses (confirmed against Julia's actual files) exactly matches Figma's paint order: lower in the Layers panel = earlier in the walk = further back; higher in Layers = later = further front. `api/_lib/figma-import.js` carries this through to `zone.zIndex` on both the plugin path (reads the plugin's own tag) and the REST path (`allNodes.indexOf(node)`, kept for parity though only the CLI script still uses that path). `TemplateCanvas.jsx`'s hardcoded reorder block (guides→text→overlap→photo-on-top, rebuilt twice earlier today) is replaced with a single generic `applyZoneStackingOrder()` helper: sorts every zone (text and image together, not partitioned by type) by `zone.zIndex ascending` and stacks them in that order, falling back to each zone's plain position in `config.zones` (its declaration order) when `zIndex` is missing - covers every already-imported record plus Option A/B, which are hardcoded in `templateZones.js` and never went through Figma import. Checked both existing `overlapAbove` zones (Option A/B's photo) are already declared after their headline zone in that array, so the fallback doesn't silently change their established photo-over-headline overlap. WildCast's own editing guides still always end up the true topmost layer after, regardless of content z-order, so zone boundaries stay visible while editing.*

*Verified live end-to-end, not just read through: mocked an import with 4 deliberately-overlapping zones (2 text, 2 image) and explicit `zIndex` values matching Julia's own example (sticker=3, photo=2, headline=1, sub_headline=0), opened it via the "Test draft" button, typed the text and uploaded two solid-color swatches through the real upload flow, and confirmed the actual rendered stacking matched exactly - sticker covering photo covering headline covering sub_headline, precisely as requested. **To use it**: arrange layers in Figma's Layers panel in the order you want them to render (top of panel = front), then run the plugin import as usual - no code change or Claude round-trip needed for a different order on this or any future template. The current live Option C record predates this feature (imported before `zIndex` existed) and will use the declaration-order fallback until re-imported.*

*Same message, one more small fix: **Position nudge added to Headline and Sub-headline in guided mode** (same commit). Offer already got this exact capability on 2026-09-08 (guided mode's canvas is locked - no drag - so nudge is the only way to fix an overlap without switching to Designer mode) but Headline/Sub-headline were never extended the same way, only the separate "restricted review" mode had it for them. One-line fix matching Offer's existing pattern exactly - `onNudge={(isNonDesigner || restricted) ? ... : undefined}`.*

*Update 2026-09-11 (cont'd) — Julia dropped a curated catalogue preview tile for Option C (`public/templates/preview_opt-c.png`, real AKKO Chicken & Grilled design, same 1191×1679 as Option A/B's existing `preview_opt-a/b.png`). Turned out a Figma-imported slot had no way to use a dedicated preview image at all - `TemplatePicker.jsx`'s `overlayCustomCards()` always set `thumb: designerCard.thumb` unconditionally, defaulting every imported template's catalogue tile to its raw Figma background PNG, with nothing checking for an override the way Option A/B's hardcoded `thumb` already worked. Fixed generically, not as an Option-C-only special case: `BASE_TEMPLATES`' Option C entry now carries its own `thumb: '/templates/preview_opt-c.png'`, and the merge logic changed to `thumb: slot.thumb ?? designerCard.thumb` - a slot's own curated thumb wins when set, falls back to the imported background otherwise. Any future Figma-imported template gets the same capability for free by just adding a `thumb` field to its `BASE_TEMPLATES` entry. Verified live: mocked Option C as a live imported record with a deliberately different (and obviously-wrong-if-used) `backgroundUrl`, confirmed the catalogue card showed the real curated preview image instead. Commit `3bd5797`.*

### New home/landing page (2026-09-11)

Julia's ask: the front page (the brief form + "Pick your template" button, always visible on load) becomes a real choice screen instead - "Start from scratch," "Choose a template," "See design library." Discussed the shape first (she confirmed: keep the existing left-column hero copy as-is, just remove the "Pick your template" step-1 button from it; the right side gets the 3 new buttons instead of the form; "+ New Brief" in the nav should land on this new home screen, not go straight into the form like before).

- **`screen: 'landing'` is now the real default** (`useState('landing')`, was `'brief'`) - the first thing anyone sees.
- **New `LandingPage.jsx`**: same outer wrapper/grid as `BriefingForm.jsx` (so the transition into the brief flow doesn't visually jump) - left column reuses `BriefingForm.jsx`'s own `HeroColumn` (now exported, with a new `showTemplateStep={false}` option so the "Pick your template" step doesn't render here), right column is 3 clickable cards:
  - **Start from scratch** → `screen: 'brief'` - the exact existing picker+form flow, completely untouched, just entered via this button instead of always being on screen.
  - **Choose a template** → `screen: 'catalogue'` - same Templates catalogue page "Templates" in the nav already opens.
  - **See design library** → `screen: 'designs'` - same Designs page "Designs" in the nav already opens.
- **"+ New Brief" nav item now lands on `'landing'`** instead of `'brief'` directly (Julia's explicit call) - still does the exact same full-state reset it always did (clears `briefSubmission`, `savedCandidateIds`, `completedFormats`, bumps `briefResetKey`), just the destination screen changed.
- Verified live end-to-end, not just read through: clicked all three new buttons from a fresh load and confirmed each lands on the right screen: Start from scratch → the untouched brief form with "Pick your template" still there; Choose a template → the real Templates catalogue; See design library → the real Designs page. Also clicked "+ New Brief" from a different screen and confirmed it returns to the new landing page. Checked mobile width (375px) - the grid collapses to a single column exactly the same way `BriefingForm`'s own grid already did, no new responsive issue introduced.
- Commit `081f00b`.

**Follow-up, same session**: Julia asked for the logo to go to the new landing page too, same destination as "+ New Brief" - it was initially left resuming whatever brief/picker was in progress (the pre-existing behavior). Changed `onLogoClick` to `setScreen('landing')` unconditionally. Deliberately does NOT also do New Brief's full state reset (clearing `briefSubmission`/`savedCandidateIds`/etc.) - clicking the logo isn't an explicit "start over" the way New Brief is, just a "go home" navigation. Verified live: opened Templates, clicked the logo, confirmed it landed back on the new 3-button home screen. Commit `d4bd5e3`.

**Follow-up, later session**: two more gaps in the landing/brief handoff. (1) Once on `screen: 'brief'` via "Start from scratch," there was no way back to the landing page short of the logo click or "+ New Brief" (which also resets in-progress answers) - added a "← Back" button (`onBack` prop, wired to `setScreen('landing')`) inside `BriefingForm`'s "Brief your design" card, next to its heading. (2) `Header.jsx`'s nav highlighted no item at all while on the landing screen, even though landing is exactly where "New Brief" itself lands you - `navItem`'s active check now also treats `target === 'new-brief'` as active when `screen === 'landing'`. Both verified live (Playwright against `vercel dev`): Back button returns to landing with the 3 choice cards visible again; "New Brief" shows highlighted on initial load at `/`.

### Client/internal-meeting follow-ups (2026-09-15, worked one item at a time, each proofed before moving on)

**1. Wild Scale callout made much more prominent.** The "Before you upload" tip box on the landing/brief hero column had the Wild Scale link as a small underlined inline text fragment inside a paragraph - Julia: "make this more prominent, wild scale should be much bigger and the button should be in your face." Redesigned into a bigger card (bolder heading, more padding) with the link as a full-width solid button of its own, not text. Lives in `BriefingForm.jsx`'s shared `HeroColumn`, so both the landing page and the brief form show the update identically. Commit `1a4f0d4` - merged cleanly with Anang's concurrent "Back to landing" button work in the same file (different region, no conflict).

**2. Designs page: "Find a design" popup replaced with an inline filter bar, matching Library.** Julia pointed at `LibraryPage.jsx`'s always-visible "Viewing" filter row as the reference and asked for the same treatment on Designs, instead of the modal that blocked the whole list until submitted. `DesignsPage.jsx` rewritten: three `useState` filters (format/merchant/date) replace the single `filters` object gated on `null`, all shown inline under the header exactly like Library's bar. Commit `06690b8`.

**Real bug surfaced during that redesign, not just cosmetic**: Julia flagged the OLD filter wasn't showing "Wen Cheng" designs at all. Checked the live saved-project data directly (`curl /api/save-project`) rather than guessing - the same real merchant had been saved under 4+ different spellings over time (`Wen Cheng`, `WEN CHENG`, `WEN CHEN`, `Wen Chen`, plus a `Wen Chen - Restaurant Flyer · Option B` fallback for a project with no `restaurant_name` set at all), each showing as a fully separate filter option, so picking any single one only ever showed a fraction of that merchant's real designs. First pass merged case-only differences (`Wen Cheng`/`WEN CHENG`). Julia then asked for real typo-tolerance too ("even if there is a spelling mistake no matter how small") - added a small shared `src/lib/fuzzyMatch.js` (conservative Levenshtein edit-distance check, digit-aware so sequential names like "Flyer 2"/"Flyer 3" never accidentally merge) and switched the merchant-option grouping to use it. Found and fixed a real bug in the grouping itself during verification: a straight frequency-then-alphabetical tiebreaker could pick an ALL-CAPS typo as a group's displayed label when two spellings tied on save count (`"WEN CHEN"` vs `"Wen Cheng"` both saved twice) - added a shouting-case tiebreaker so the properly-cased spelling always wins the display label. Verified against real production data pulled via the live API (not synthetic test data) before and after each change. Commit `3b22c26`.

**3. Non-blocking "Did you mean...?" hint added at the point typos actually happen** - the editor's free-text Restaurant name field (`FieldEditor.jsx`), which had zero autocomplete/validation before this. Julia chose the inline-hint version over a blocking popup when asked directly (two concrete mockup-style options put to her). Checks the typed value against `PLACEHOLDER_PARTNERS` (`briefConstants.js`) using the same `fuzzyMatch.js` helper; only shows when the value is close-but-not-exact (never while already correct or too different to plausibly be the same name). Verified live: typing "Wen Chen" surfaced "Did you mean Wen Cheng? Use this," clicking it filled the correct spelling and the hint disappeared. Same commit as above (`3b22c26`).

**4. Actual bad data cleaned up, not just the display/filter layer patched.** Asked Julia which spelling should be canonical before touching anything (her own `PLACEHOLDER_PARTNERS` list said `'McD'`, but real saved designs were split `McDonald's`/`McDonalds`/`MacDonalds`/`McD` - genuinely ambiguous which was "right," so asked rather than guessed) - she chose **"McDonald's"**. Wrote a one-off script (fetch each stray record's full JSON via `load-project.js`, patch only `fields.restaurant_name`, save back via `save-project.js` - never touched anything else in these records) and fixed 16 live saved designs: 12 McDonald's-group records (`McDonalds`×6, `MacDonalds`×2, `McD`×3, plus the one fallback-string record) → `McDonald's`, and 4 Wen-Cheng-group records (`WEN CHEN`×2, `Wen Chen`×1, plus its one fallback-string record) → `Wen Cheng`. Verified via a fresh `GET /api/save-project` after: 17 `McDonald's` + 6 `Wen Cheng`, zero stray spellings left except the one deliberately-untouched case-only `WEN CHENG` (already handled by the fuzzy filter, no data change needed for that one). Hit the documented Vercel Blob `list()`-after-rapid-writes lag immediately after the 16 writes (a stray record briefly still showed its old spelling in the list endpoint while a direct `load-project.js` fetch of that same record already showed the correct new value) - confirmed self-resolving within ~30s, not data loss, matching the same known characteristic documented elsewhere in this file. Also updated `PLACEHOLDER_PARTNERS` in code (`'McD'` → `"McDonald's"`) to match. Commit `9270cb8`.

**5. "Choose your mode" options renamed to just "Guided" and "Designer".** Was "Text + Image · Guided" / "Text + Image · Designer" - redundant prefix since the text-only variants are already hidden (`disabled: true`) and never shown alongside these two. `TemplatePicker.jsx`'s `LayoutModal`. Verified live. Commit `582b0cc`.

**6. Team sign-in (email + password), stage 1 of personal folders per seat.** Julia's ask, discussed at length before building since it's a real architecture decision: the Wolt team wants a 5-seat test group plus her own Wild Stack team to each have personal folders in Designs, but the app currently has zero concept of individual people - everyone using a shared activation key (e.g. "Wild Stack") looks identical to the system. Confirmed with Julia before building: (a) real per-person accounts, not just a self-declared name; (b) password-only for now, no email verification codes (that needs an email-sending service this app doesn't have - a genuine prerequisite, not something to fake); (c) self-service signup, not pre-provisioned - accounts don't exist until someone signs in with a new email for the first time, and THAT sign-in creates the account there and then, using whatever password they typed as their password going forward; (d) role assigned automatically by email domain (`@wildstack.studio` → `agency`, same as Wild Stack's own shared key already grants; anything else → `partner`) rather than asked - matches "let's make it more global for our team."

New API routes: `api/account-auth.js` (signup-or-login in one call - looks up the email in Vercel Blob `accounts/<safe-email>.json`; not found = create it now with the given password; found = verify the password against it) and `api/account-session.js` (re-validates a stored `{email, sessionToken}` pair on page reload, mirroring how the existing activation-key flow already does this - never sees a password, only compares an already-issued token). Passwords hashed with Node's built-in `scrypt` (no new dependency), verified with `timingSafeEqual`. `ActivationGate.jsx` gets a second tab ("Team sign in") alongside the completely-unchanged "Activation key" tab - email + password, with a name field that only appears once the server's confirmed this specific email has never signed in before (two-step: first submit without a name gets told "new account, name required"; the actual account only gets created on the follow-up submit that includes one). `App.jsx`'s activation state/restore-on-mount now branches on a new `wildcast_auth_type` flag between the two paths, but both produce the exact same `{key, clientName, credits, role}` shape, so nothing else in the app (role-gating, credits, etc.) needed to change. `Header.jsx`'s sign-out now clears both possible localStorage sets, and its confirmation copy no longer assumes everyone signed in with an activation key.

Verified end-to-end against the real production API (not mocked) before shipping: new-email signup correctly demands a name first; second submit with a name creates the account with the right role for both an `@wildstack.studio` test email (→ `agency`) and a non-Wild-Stack one (→ `partner`); wrong password correctly rejected (401); correct password login issues a fresh session token and correctly invalidates the previous one; session-restore endpoint correctly accepts the current token and rejects the stale one. Also clicked through the real UI on production (not just the API) - signed in, reloaded the page to confirm the session survives a refresh, signed out, confirmed every related localStorage key was actually cleared. Test accounts created during this verification were deleted afterward via a short-lived cleanup route added and removed in the same session, not left as permanent surface area. Commits `26c9b8d`, `cb4c8c0`, `53fab73`.

**Not yet built**: the actual personal-folder UI in Designs (main folder named after the signed-in person + subfolders they create, everyone can browse everyone's folders per Julia's call) and the seat/person filter in Designs search - this entry is just the identity foundation those need. Also still open: Julia's "more global for our team" framing was interpreted as the domain-based auto-role-assignment described above - flagged to her for confirmation, not yet explicitly confirmed as of this entry.

**7. Domain-role interpretation confirmed; 5-seat cap + seat counter; export credits replaced with AI-only credits.** Julia confirmed the `@wildstack.studio` = global/uncapped reading from item 6 was correct, then asked for three more things in the same message: (a) hard-cap the Wolt test group at exactly 5 seats, no 6th signup allowed; (b) somewhere visible showing how many seats are left; (c) remove the PDF-export credit system entirely and replace it with a new "AI credits" system starting at 100, ticking down only on AI feature use (AISuggest/"Improve with AI"), never on export.

**Seat cap**: new shared `api/_lib/accounts.js` (`listAccounts()`, `countPartnerSeats()` - fetches every `accounts/*.json` blob and counts `role !== 'agency'`, i.e. everyone outside the wildstack.studio domain). `api/account-auth.js`'s signup path now checks this count before creating a brand-new `partner`-role account and rejects with a 403 once 5 already exist (`"All 5 team seats are taken."`) - checked only on first-ever signup, never on a returning login, so the 5 who already have accounts keep working after the cap is hit; `agency` (wildstack.studio) accounts are never counted or capped. New public `api/account-seats.js` (GET, no auth - it reveals nothing but a count) returns `{used, total, remaining}`, shown on `ActivationGate.jsx`'s "Team sign in" tab as a small banner ("N of 5 team seats available" / "All 5 team seats are taken") that refetches whenever that tab is opened.

**Credits**: `App.jsx`'s `handleExport` no longer checks or decrements `activation.credits` at all - PDF export is now free for every activation type (key-based and account-based alike). `handleAiCreditUsed` (wired to every `AISuggest`/"Improve with AI" button via `FieldEditor.jsx`) is unchanged and is now the only thing that spends a credit. New account signups' starting balance dropped from 1000 → 100 (`ActivationGate.jsx`'s `ACCOUNT_DEFAULT_CREDITS`, and the matching fallback in `App.jsx`'s session-restore effect). UI label changed from "N exports remaining"/"N credits" to "N AI credits" in both the editor breadcrumb (`App.jsx`) and the header pill (`Header.jsx`), so it's clear what's actually being metered. Key-based clients' `WILDCAST_KEYS`-configured credit totals are untouched (Julia manages those directly via the env var) - only what decrements them changed.

Verified against real production, not mocked: created 5 test partner signups (all succeeded), a 6th correctly rejected with the seat-cap error, confirmed `/api/account-seats` reported `used:5, remaining:0`, confirmed a `wildstack.studio` signup still succeeded past the cap and didn't move the partner count, confirmed the "Team sign in" tab rendered "All 5 team seats are taken" live. Logged in as one of the test accounts and confirmed the header showed "100 AI credits" (not 1000, not "exports"). All 6 test accounts deleted afterward via a short-lived cleanup route added and removed in the same session (`/api/account-seats` confirmed back to `used:0` after). Commits `8d70976`, `2b8cf5a`, `b2ff2f7`.

**8. Personal folders per seat, stage 3 - the actual folder UI in Designs.** Closes out item 6/7: every saved design is now stamped with who saved it and can be filed into a freeform subfolder they create themselves.

**Data**: a saved project record gains `ownerEmail`/`ownerName` (whichever signed-in identity saved it - an account email + display name, or a shared activation key + client name, unified since both already produce the same `{key, clientName}` shape - see item 6) and `folder` (a subfolder name, or `null` for the owner's unsorted root). `App.jsx` stamps these at save time and now preserves them correctly across re-saves via new `projectOwner`/`projectFolder` state, loaded from whatever a re-opened project already had - re-saving an existing design (even by someone else editing it in place, since Designs has no privacy boundary) never silently reassigns ownership or drops it out of its folder. Duplicating a design explicitly resets both to the CURRENT user and unsorted, since a duplicate is a fresh personal copy, not a continuation of the original's filing.

**API**: new `api/folders.js` (a small per-owner registry blob, `folders/<safe-email>.json`, listing subfolder names they've created - kept separate from the designs themselves so an empty, just-created folder still shows up before anything is filed into it) and `api/move-project.js` (files/un-files one design into a subfolder - a dedicated endpoint rather than round-tripping the whole project through the client, since Designs' list view only ever holds lightweight summaries and re-POSTing that through `save-project.js` would silently wipe every field the summary doesn't carry).

**UI**: `DesignsPage.jsx` gains a view toggle - the existing flat "All designs" list (now also with a **Person filter** in the Viewing bar, directly answering "filter by seat/person") alongside a new **"Folders" view**: a grid of person tiles (name + design/folder counts, always includes the current signed-in person even with zero designs yet) → click into one to see their subfolders + an "Unsorted" section + a "+ New folder" button → click a subfolder to see just its designs. Every design card also gets an inline "file into folder" dropdown (Unsorted / existing folders / "+ New folder…") right on the card, so moving something doesn't require leaving whatever view you're in. Per Julia's explicit call (confirmed via AskUserQuestion earlier this session): everyone can BROWSE every person's folders, but only that person can create folders or move designs in their own space - the dropdown/"+ New folder" controls only render when `activation.key` matches the design's/space's owner.

Verified end-to-end against real production, not mocked: created a test account, created a folder for it via the API, saved a test design stamped with that account's identity, moved it into the folder via the API, confirmed the list endpoint reflected the owner/folder correctly. Then walked the actual UI on production: signed in as the test account, confirmed the "by Folder Test Person" owner badge and the Person filter (correctly showing only that one real account among the many un-owned legacy designs) on the All-designs view, switched to Folders, confirmed the person tile ("1 design · 1 folder"), drilled into it (folder tile + correctly-empty "Unsorted (0)"), and drilled into the folder itself to see the design. Test account, test design, and the folder registry blob all deleted afterward via a short-lived cleanup route added and removed in the same session. Commits `c8a0233`, `b502d9f`, `c337466`.

**Note**: designs saved before this shipped have no `ownerEmail` and so won't appear under anyone's folder (only in the flat "All designs" view, same as always) - this only applies going forward from here, not retroactively.

**12. FOGRA51 listed first; a real post-save popup; one stale-copy fix found along the way.** Three small asks/fixes, verified together on production.

- **ICC profile order**: `FieldEditor.jsx`'s `ICC_PROFILE_OPTIONS` swapped so FOGRA51 renders above FOGRA39 in the Print settings panel - display order only, FOGRA39 stays the actual default unless someone explicitly picks the other one (unchanged elsewhere: `App.jsx`'s initial state, the `?? 'fogra39'` active-check, `api/export-cmyk.js`'s own default). Verified live - FOGRA51 lists first, FOGRA39 still shows checked/active.
- **Post-save popup**: plain editor Save used to only flash a quiet 3-second "Saved" corner badge. Now also shows a "Continue editing / Create a new design / Exit" modal (`SavedModal` in `App.jsx`) - Continue editing just closes it, Create a new design does the same full reset+landing-screen flow as clicking "+ New Brief," Exit goes straight to the landing screen. Deliberately skipped whenever the existing "Need more layouts?" brief-format prompt is about to show instead (`offerMoreFormats()` now returns whether it showed its own modal) - a save never stacks two popups. Verified live: Save → popup appears with all three options; Continue editing closes it and stays in the editor.
- **Stale copy fix, found while verifying the above**: the Export PDF button still said "Uses 1 export credit — N remaining" underneath it - leftover from before item 7 removed export credits entirely. Deleted the line. Verified live - gone.

Commits `e1f00d9`, `15743db`.

**13. Optional "Project name" field added to the brief form.** Sits right after "Formats needed" (Julia's ask) - `BriefingForm.jsx` gains a plain text input (`brief.projectName`, new `DEFAULT_BRIEF` key in `briefConstants.js`), not required. `App.jsx`'s `handleSelectTemplateFromBrief` now checks it first: a typed name wins outright and is used as-is; left blank, the existing auto-tag (merchant + offer + template name) kicks in exactly as before. Verified live end-to-end on production: filled out a full brief with a custom project name, picked Option B, chose Guided mode, and confirmed the editor's own Project Name field showed the exact typed text, not the auto-generated tag. Commit `36081b4`.

**14. Nav order swap + Designs card alignment/truncation.** Julia's ask: swap Designs and Library in the nav (now New Brief / Templates / Designs / Library / Import / Help - `Header.jsx`), and in Designs, keep every "Continue editing" button on the same line across a row even when titles differ in length, truncating a long name with an ellipsis and showing the full name on hover like a native image tooltip. `DesignCard` in `DesignsPage.jsx`: the card is now a flex column at `height: 100%` (fills its CSS Grid row, which stretches to the row's tallest card by default), with the button/folder-select block pinned to the bottom via `marginTop: 'auto'` - the button lines up regardless of how much title/owner text sits above it. The title itself switched from wrapping (`wordBreak`) to single-line ellipsis truncation with a native `title=""` attribute carrying the full name, which is also most of why heights varied in the first place. Verified live on production: all four cards in a row now show their "Continue editing" button on the identical line, and long titles ("McD — Restaurant Flyer · Option B", "Wen Cheng — Restaurant Flyer · Option A") truncate to one line instead of wrapping.

Also answered two related questions from the same message: (1) **"+ New folder" location** - it's one level in from the top-level person grid, inside a person's own folder view (click their tile first) - confirmed still working correctly post-change. (2) **Import access** - confirmed `role === 'agency'` is the only gate (`Header.jsx` + `App.jsx`'s `handleNavigate`/render check), auto-assigned only to `@wildstack.studio` account signups (`api/account-auth.js`); for the older shared-key system the role is whatever Julia herself set per-key in `WILDCAST_KEYS` (`key|ClientName|credits|role`, defaults to `partner`) - not something the code can audit from here, flagged for her own check. Commit `01d06ed`.

**Same-day follow-up: "i dont see new folder and i dont see hover state name?"** Investigated live rather than guessing - re-tested both on production myself, including specifically at 375px (mobile) width, since every screenshot Julia's sent all day has been phone-width. Both were actually working correctly: "+ New folder" renders exactly as designed once you tap into your own person tile (re-confirmed at mobile width too - not a regression). The hover tooltip, though, genuinely cannot work on a phone or tablet at all - hover has no touch equivalent, so a native `title=""` tooltip (however correctly implemented) can never fire there, on any website, not just this one. That's the real root cause, not a bug.

Fixed properly rather than just explaining it away: tapping a truncated design name now shows the full name via a plain alert - works identically whether the click came from a touch tap or a mouse, and `stopPropagation` keeps it from also triggering the card's own "open this design" action. Verified live on production at mobile width: tapping the truncated "Wen Cheng — Restaurant Flyer • Optio…" title did NOT open the design's confirm-open modal (proving the tap was correctly intercepted before reaching the card's own click handler) - the alert itself can't be screenshotted (native browser dialogs never render in a page screenshot, same limitation hit earlier with `window.prompt` during the rename-feature work), but the interception behavior confirms the handler fired as intended. Commit `760d610`.

### 2026-09-16

**16. WildScale button rename.** The "Prep your assets with Wild Scale →" button (BriefingForm.jsx's hero tip box, and its equivalent in TemplatePicker.jsx) now reads "WildScale" - one word, matching the product's actual name. Surrounding prose mentions of "Wild Scale" left as-is, she asked specifically about the button. Verified live. Commit `bf94476`.

**17. De-branded the Wolt-specific eyebrow/tagline; added an AI credits tooltip.** Three asks. (1) Removed the "WOLT PARTNER TOOLS" eyebrow label above the "We help {design/export/print}" headline (`BriefingForm.jsx`'s shared `HeroColumn`, used by both the landing page and the brief form) and above "All templates" on the Templates catalogue page (`TemplatePicker.jsx`'s `CatalogueView`) - left the equivalent text in `TemplatePicker.jsx`'s unreachable `mode="hero"` block untouched since it's dead code, never rendered (confirmed no caller passes that mode). (2) Replaced every "Print templates in minutes." tagline with "Print Templates in Minutes" - matching WildScale's own tagline style instead of naming Wolt specifically: the sign-in gate's subtitle and footer (`ActivationGate.jsx`), the main app's footer (`App.jsx`), and the browser tab title (`index.html`'s `<title>`). (3) The "N AI credits" pill now has a hover tooltip (both `Header.jsx`'s nav pill and the editor breadcrumb in `App.jsx`) explaining what AI credits are actually for and that PDF export doesn't use them - matching WildScale's own use of tooltips, per Julia's ask. Verified live on production: eyebrow confirmed gone from both the landing page and Templates catalogue, tagline confirmed on the sign-in gate/footer/tab title, and the tooltip text confirmed present on the credits pill via direct DOM inspection. Commit `22f720e`.

**18. Footer matched to WildScale's exactly.** Inspected scale.wildstack.studio's live footer directly (its actual rendered markup, not a guess) - a single centered "© <year> Wildstack Studio" line: max-w-6xl (1152px) container, px-6 py-6 (24px padding), text-xs (12px), text-gray-400 (which is `var(--light)`'s exact hex in this app already), text-center. Replaced both of WildCast's own footers (the main app in `App.jsx` and the sign-in gate in `ActivationGate.jsx`, previously a two-item space-between row reading "WildCast - Print Templates in Minutes." / "Built by Wild Stack") with the same markup and styling. Verified live via direct DOM inspection - the rendered footer HTML matches Scale's structure and shows "© 2026 Wildstack Studio", centered. Commit `498e5ba`.

**19. "Choose a template" only shows real templates; gained a Go back button.** `BASE_TEMPLATES` (`TemplatePicker.jsx`) is a fixed 30-slot skeleton for every category/format WildCast will eventually support - only "Restaurant Flyer" actually has live templates today, but every other slot (Poster, Wild Poster, Retail...) rendered as a "Coming soon" placeholder tile on the catalogue page. The default-exported `TemplatePicker` component now filters its groups to `liveCount > 0` before ever reaching `CatalogueView` - only real, built templates show. Also added a "← Back" button to that page (same style as `BriefingForm.jsx`'s own), wired in `App.jsx` to return to the landing page - there was previously no way back except the nav bar or the logo. Verified live on production: "Choose a template" now shows only "Restaurant Flyer Templates," no Coming Soon tiles for any other category; clicking "← Back" returns to the landing page. Commit `a877d19`.

**20. "WildCast" branding consistency.** Julia's ask: no space in the product name, everywhere. Grepped the whole codebase for "Wild Cast" - only one occurrence in user-facing text, the sign-in gate's own `<h1>` heading; everything else (title tag, both footers) already said "WildCast" correctly. Fixed that one. Verified live via screenshot. Commit `b3d5789`.

**21. Designs/Library polish batch.** Six small asks in one message. (1) Nav renamed: "Designs" → "Design library", "Library" → "Assets" (`Header.jsx`). (2) `LibraryPage.jsx`'s own page heading also changed to "Assets" - she asked for both the nav label and the page itself. (3) Landing page card "See design library" → "See Design library" (`LandingPage.jsx`). (4) The All designs/Folders toggle on Designs (added item 15's coral outline) was stretching to the full width of its container - a `display:'flex'` div is block-level by default, so without an explicit width it fills its parent; added `width:'fit-content'` so it now bounds tightly to its two buttons. (5) Removed the "All dates" filter from Designs' Viewing bar entirely - the `dateFilter` state, its `dateOptions` derivation, its clause in the `filtered` memo, its entry in the active-filter summary, and the now-unused `dayKey` helper. (6) Both `DesignsPage.jsx` and `LibraryPage.jsx` gained a "← Back" button (same style as the Templates catalogue's own, added in item 19) wired in `App.jsx` to return to the landing page - neither page had a way back before except the nav bar or logo. Verified live on production: nav labels, page heading, landing card text, toggle width, missing date filter, and both back buttons (clicking either correctly returns to landing) all confirmed. Commit `b27a06c`.

**22. Follow-up: Designs page's own heading also renamed.** Item 21 only changed the nav label and left the on-page `<h1>` reading "Designs" - Julia pointed at it directly and asked for it to say "Design library" too. Fixed (`DesignsPage.jsx`). Verified live. Commit `d5bb3c1`.

**23. WildScale button un-bolded; real out-of-credits popup.** Two asks. (1) `BriefingForm.jsx`'s "Prep your assets with WildScale" button - Julia felt bold looked worse, changed `fontWeight` 800 → 400. (2) The AI-credits-exhausted gate in `AISuggest.jsx` used a plain browser `alert()` - Julia asked for a real notification box like WildScale's. Checked WildScale's live site for its own version first rather than guessing - couldn't reach a genuine depleted-credits state there (no test account with 0 credits), only saw their inline "Invalid activation key" form error, a different UI. Built a proper centered popup instead, matching WildCast's own existing modal language (same style as `SavedModal`/`ConfirmOpenModal` - dark overlay, white rounded card, clear message, single "Got it" dismiss) rather than guess-copying Scale's exact pixels; flagged this honestly to Julia rather than claiming an exact match. Verified live end-to-end: set a test session's credits to 0 via localStorage, opened a design, clicked AI Suggest, confirmed the new modal appeared (not a browser alert) with working dismiss, then restored the test session's credits. Commit `e2be925`.

**24. Follow-up: out-of-credits popup wording matched to WildScale exactly.** Julia asked directly whether WildScale actually has a popup for this - checked properly this time (fetched and searched their live bundled JS for the real string, not just re-testing the login-error path) and found the honest answer: no, WildScale doesn't use a popup at all, just a plain inline red `<p>` below their export button reading "Out of credits. Contact Wild Stack to top up." Told Julia this directly rather than let the earlier "couldn't verify" answer stand uncorrected. She chose to keep WildCast's popup (more prominent) but match Scale's exact wording - changed the body copy from "You've used all your AI credits. Contact Wild Stack to get more." to "Contact Wild Stack to top up." (`AISuggest.jsx`). Verified live: depleted a test session's credits again, confirmed the popup now reads exactly "Out of AI credits" / "Contact Wild Stack to top up.", restored credits after. Commit `272b089`.

**25. Landing page: "Start from scratch" → "Create from brief".** Julia's ask (`LandingPage.jsx`). Verified live: card reads "Create from brief," clicking it still correctly opens the brief form. Commit `962025e`.

**26. "Pick your template" (brief Step 1) now shows every live template automatically - the actual fix for "needs manual linking."** Root cause: `TemplatePreviewModal.jsx` had its own hardcoded 2-option list (`GROUPS`, sourced from `CANDIDATE_TEMPLATE_IDS`) completely separate from the real live-template system `TemplatePicker.jsx`'s catalogue already used - so Option C (already live, already showing correctly on the Templates catalogue page since item 19) never appeared in the brief flow's own template picker, and any future Figma import would have needed this file hand-edited too, exactly as Julia flagged. Rebuilt on the same `overlayCustomCards()`/`deriveGroups()` logic as the catalogue (both now exported from `TemplatePicker.jsx` instead of being catalogue-only internals), filtered to live members, rendering every group/option that actually exists. `BriefingForm.jsx`'s `pickedOption` preview (the small thumbnail shown next to "Step 1" once something's picked) now resolves via `entryForGuidedId()` - the same overlay resolution - instead of its own now-deleted flat lookup table, so it can never drift out of sync with what the picker itself offers again.

Verified live end-to-end on production, not just that the option appears: opened "Create from brief," clicked "Pick your template," confirmed Option A, B, and C all show (previously only A/B); selected Option C, confirmed the hero's "Step 1" area updated to "Option C selected" with the real thumbnail; filled out the rest of the brief (partner, business type, about, objective, Flyer format) and clicked Continue; confirmed "Choose your mode" opened correctly reading "Restaurant Flyer · Option C" (not a fallback screen); picked Guided mode and confirmed the real editor opened with Option C's actual template loaded - correct breadcrumb ("Restaurant Flyer · Option C · A6 · Guided"), correct auto-filled project name, and real Headline/Sub-headline fields, not blank or broken. Commit `5f0fa72`.

**Same-day correction**: Julia clarified she's actually on her laptop the whole time, not mobile - the "hover has no touch equivalent" diagnosis above was therefore the wrong root cause (the real explanation, if the click-reveal fix above still didn't show up for her, needs fresh investigation rather than another device guess - it wasn't re-litigated further since she moved on to other asks in the same message, see below).

**15. Folder auto-creation on signup; bigger/coral-outlined view toggle.** Two more asks from the same message. (1) Julia asked directly whether a new seat's folder needs manual creation or happens automatically on sign-in - checked the code rather than assume: it did NOT happen automatically - a person's tile in Designs' Folders view only ever appeared once they'd saved a design or opened their own Folders tab and created a subfolder, so a seat who'd only just signed up and done nothing else was genuinely invisible to everyone else. Fixed: new `ensurePersonFolder(email, name)` in `api/_lib/accounts.js` creates an empty folder-registry record (idempotent - no-ops if one already exists), called from `api/account-auth.js` on both a brand-new signup AND every existing login (the login case backfills anyone who signed up before this shipped, on their next sign-in). `api/folders.js` now imports the same `folderPath` helper instead of keeping its own duplicate copy. Verified live: signed up a fresh test account via the API, immediately queried `/api/folders`, and confirmed it already had an empty-folders entry - no design save or manual folder click needed. (2) The "All designs / Folders" toggle above the Viewing bar (item 10) was easy to miss - made it bigger (larger padding/font) with a `var(--primary)` (coral) outline around the whole control and a filled coral active state instead of plain white. Verified live via screenshot. Commit `4936fce`. Test data (one signup used to verify auto-creation) cleaned up via a short-lived cleanup route added and removed in the same session, `/api/folders` confirmed empty of it afterward. Commits `9ebe070`, `5f6fe22`.

**9. Rename a design directly from the Designs tab.** Julia's ask: relabeling a design shouldn't require opening the editor just to touch its "Project name" field. Added a small pencil icon next to each card's existing delete "×" (same hover-reveal treatment) - prompts for a new name and saves it via a new small `api/rename-project.js` endpoint, same reasoning as `move-project.js`: Designs' list only ever holds a lightweight summary, so this loads the real full record server-side and patches only `projectName` rather than risking a client round-trip wiping other fields. Deliberately not owner-gated, unlike the folder-organizing controls - renaming follows the same fully-shared editing model Designs already has for everything else (anyone can "Edit original"). Verified against real production: saved a test design, renamed it via the API, confirmed the list reflected the new name; confirmed in the actual browser UI that the pencil button renders correctly on every card (hover-revealed) and its click handler fires. Commit `1f931ab`.

**10. Two follow-up fixes on the above, same day.** Julia sent a screenshot (a narrow/phone-width crop) asking "i dont see a flyer editing function am i not seeing it?" - real bug, not a missed feature: the rename/delete icons added in item 9 were hover-revealed (`opacity: 0` until `onMouseEnter`), and hover has no touch-device equivalent, so on phone/tablet they never appeared at all, at any point, no matter how you tapped the card. Made both always-visible instead. Also moved the "All designs / Folders" view toggle (item 8) from next to the page title down to its own row directly above the Viewing filter bar, per her explicit placement ask in the same message. Verified on real production at both desktop and mobile (375px) width - icons now visibly present on every card without hovering, toggle sits in the new position. Commit `a278010`.

**11. Landing page: right-side choice cards shifted down.** Julia sent a screenshot - the "Start from scratch / Choose a template / See design library" cards sat pinned flush at the top of the page (top-aligned against a much taller left hero column: headline, tip box, 4-item feature list), leaving a lot of empty space underneath them and landing above where the eye naturally goes. Changed `LandingPage.jsx`'s grid from `alignItems: 'start'` to `'center'` - a one-line, non-brittle fix (no fixed pixel offsets to keep in sync as either column's content changes) that vertically centers the shorter right column against the taller left one. Verified live on production via screenshot - the three cards now sit centered against the hero column instead of flush at the top. Commit `39343e6`.

### 2026-09-16 (cont'd)

**27. "Coming soon" removed from the Pick your template modal too.** Item 26 fixed the modal's live-template list but left its "Posters"/"Wild Posters - coming soon" placeholder sections in - Julia asked to drop those too, matching the same "only show what we have" fix already applied to the main Templates catalogue page (item 19). Removed (`TemplatePreviewModal.jsx`). Commit `dab9f67`.

**28. Real print-quality bug fixed: headline/sub-headline/offer were rendering in the wrong font weight.** Julia flagged this as urgent - "the text for the InDesign pdf and the WildCast pdf look different, more squished" - and provided her own comparison files (a real InDesign export vs WildCast's own FOGRA39/51 exports) plus the exact correct font ("WOLT Cond Black Regular"). Investigated properly before touching code: read both PDFs directly and compared the headline rendering side by side (WildCast's was visibly thinner/more condensed, a different weight entirely, not a rendering quirk), then traced it to the actual bug - every headline/sub_headline/offer/restaurant_name zone across every hardcoded template (`src/data/templateZones.js`: Option A guided+designer+text-only, Option B) has been requesting `omnes-cond` at `fontWeight: 700` (Bold) since the templates were first imported from Figma. A code comment on the file already documented why: the real weight was 900 (Black/WOLTCondBlack.otf), but that face wasn't registered as a usable `@font-face` at import time, so the script force-downgraded every zone to 700. That got fixed on 2026-09-11 (the 900-weight `@font-face` and its preload have been live and unused ever since - `TemplateCanvas.jsx` even has a comment noting it "was deployed but unused before") but nothing ever went back and updated the zone definitions to actually request it, so every export since then quietly kept using Bold. Fixed: all 8 `omnes-cond`/700 occurrences → 900, confirmed via full-file read this was every one and none were missed. PDF export needed no separate fix - `handleExport` rasterizes the live Fabric.js canvas to a PNG client-side and that's what `api/export-cmyk.js` wraps into the CMYK PDF, so whatever font the browser canvas renders is exactly what ships in the exported PDF; no server-side text/font handling exists to fix separately. Figma-imported templates (Option C, future imports) were never affected - they already read their real weight straight from the Figma file's own text styling, not a hardcoded value.

Verified live on production, not just in theory: opened Option A in Guided mode, typed the exact matching content from Julia's InDesign reference ("POTSDAMS NEUES" / "DREAMTEAM"), zoomed the canvas, and confirmed the headline now renders in thick, rounded, heavy letterforms - a clear visual match to the InDesign reference's bubbly Black-weight style, not the thinner condensed look from before. Also incidentally confirmed via the saved project's preview image (used on the review-link page) that the fix flows through to every place the design is rendered, not just the live editor. Commit `5f95c17`.

**29. Position nudge added to T&Cs in guided mode.** Julia asked for nudge/position controls on headline, sub-headline, T&Cs and offer, "on all templates." Checked first rather than assume a bigger gap existed: headline, sub-headline, and offer already had it (shipped 2026-09-08/09-11); T&Cs was the one of the four actually missing it. Added the identical `onNudge` wiring T&Cs was missing (`FieldEditor.jsx`) - gated the same way (guided/restricted mode only, since Designer mode's canvas isn't locked and drag already works there), and applies to every template with a `tc` zone automatically since that's already how the field's visibility is conditioned - no per-template changes needed. Verified live: opened Option A in Guided mode, confirmed T&Cs now shows the same ← → ↑ ↓ Position arrows as Offer directly above it. Commit `381673c`.

**30. Two-way replies + resolved tick-marks (item 26 from 2026-09-15's list, built same day it was scoped).** Investigated the existing Send for Review flow first, honestly: it was one-directional only - an external reviewer could leave comments via their share link, but the designer's Feedback sidebar in the editor was read-only, and nothing anywhere tracked whether a comment had been addressed. Confirmed scope with Julia before building (comments/tick-marks now; email notifications explicitly deferred - she flagged that part herself as not yet scoped, since who gets notified depends on the seat/account system still being built out).

`api/comments.js`: every comment now carries `from` (`'reviewer' | 'designer'`, defaults to `'reviewer'` so old comments and the external ReviewPage.jsx form - which never sends it - keep working unchanged) and `resolved` (boolean). New `PATCH` handler flips `resolved` on one comment by id. `GET` now cache-busts its blob read, same recurring CDN-staleness fix used throughout this project, since both sides now read/write in the same short back-and-forth.

`App.jsx`'s editor Feedback sidebar gained a reply box (posts as `from:'designer'` using the signed-in activation's own name - no separate name field needed, unlike the reviewer's own form) and a "Done" checkbox per comment that dims + strikes it through once resolved. `ReviewPage.jsx` (the external share-link page) got the same "Done" checkbox, plus designer replies are visually tinted and labeled "· designer" so a reviewer can tell them apart from their own comments.

Verified thoroughly end-to-end on real production, not mocked: saved a test design, posted a comment on its review link as a simulated external reviewer, confirmed it appeared in the editor's Feedback sidebar on next load (comments only fetch on load/reopen, no live polling - expected, not a bug), replied from the editor sidebar and confirmed the reply posted correctly tagged and tinted, ticked "Done" on both comments and confirmed the resolved state round-tripped correctly to the review page (labeled/tinted designer reply included). One real scare during testing, run to ground rather than assumed away: newly-toggled `resolved` state didn't show up on an immediate re-read after the PATCH - looked like a bug at first, but a direct manual PATCH+read via the API worked instantly, and the UI-triggered writes DID eventually show up correctly on their own (once ~10s, once ~25s, once ~30s later) with no additional action taken - this is the same documented Vercel Blob list()-read-after-write consistency lag this project has hit and confirmed self-resolving several times before (STATUS.md, 2026-07-28/29), not a new bug, just landing at the slower end of its known range under rapid automated back-to-back testing. Test design and its comments deleted afterward. Commit `870fcc9`.

**31. Independent re-verification pass on items 28-30 (font fix, T&C nudge, comments/replies), fresh production session.** Julia asked for one more confirmed-working pass specifically on the font weight fix, since it's the print-quality issue she called urgent. Signed in fresh on `cast.wildstack.studio`, opened Restaurant Flyer · Option A in Guided mode, typed "DREAMTEAM"/"POTSDAMS NEUES" - headline and sub-headline both rendered in the thick, rounded Black weight, a clear visual difference from the old Bold rendering. T&Cs step confirmed showing the same ← → ↑ ↓ Position row as Offer above it. Ran the full comments/replies/resolved-tick loop again end to end (reviewer comment → designer reply, tinted and labeled "· designer" → Done ticked on both sides → resolved state and reply both appeared correctly on the external review link after the same known Blob-propagation delay, ~10s this time). All three confirmed working with no regressions. Test project deleted afterward.

**32. "Choose from library" picker: "All merchants" filter showed "No assets match this filter" even though assets existed.** Julia reported this directly from a real McDonald's design. Reproduced live: opened the Restaurant logo field's library picker on that design, and the "All merchants" dropdown showed nothing despite the library genuinely having a McDonald's logo (confirmed via the raw `/api/library-assets` response). Root-caused by inspecting the component's actual live state rather than guessing: `FieldEditor.jsx`'s `openLibrary()` pre-selects the merchant filter to the design's free-text `restaurant_name` field value ("McDonald's") every time the modal opens - but assets are tagged with whatever (often abbreviated, e.g. "McD") merchant string a partner typed at upload time. When that text doesn't match any real tag, a native `<select>` with no matching `<option>` silently falls back to *displaying* "All merchants" (the first option) while React's own state stays on the unmatched "McDonald's" - so the list still filters using that stale, invisible value and finds nothing, while the dropdown visibly (and misleadingly) shows "All merchants" selected. Fixed: `openLibrary()` now only defaults to the restaurant name when it's an actual existing merchant tag, otherwise defaults to "All merchants" for real. Verified live on a duplicated copy of the same McDonald's design: opening the logo library picker now correctly lists both the McDonald's and Wen Cheng logos under "All merchants". Commit `c9f0e46`.

**Also investigated the same day: reported "mirroring" on the McDonald's logo and Wolt bag in exported PDFs - confirmed NOT a bug.** Julia sent zoomed print-preview crops from a Fogra51 export showing what looked like a mirrored/ghosted duplicate right at the edge of both images. Rendered the actual PDF at high resolution and traced the code: `api/export-cmyk.js` fills the 3mm bleed margin by mirroring the trim-edge pixels outward (`sharp().extend({ extendWith: 'mirror' })`) rather than stretching the artwork - a standard, intentional print technique so nothing in the design gets distorted, and the mirrored strip is inside the bleed area that the printer trims away. It shows up specifically on the logo and the Wolt bag because those are the only two elements in this template documented to intentionally bleed past the trim line (`templateZones.js`, Option B). No code change - explained the bleed technique to Julia instead.

**2026-09-21 - Fixes from Julia's Prompt Brief / Import / Designs testing.**
- **Prompt Brief dropped sticker + QR (and any extra zone).** The chat collected them, but `assembleBrief` only forwarded logo and photo, `buildCandidateFields` hardcoded `qrUrl: null`, and the blob-to-data-URL step on save only covered `logoUrl`/`photoUrl`/`qrUrl`. Now every other image answer travels as `brief.zoneImageUrls` and lands on `<zoneId>Url` (what the canvas reads), extra text zones (e.g. a promo "code" box) travel as `brief.zoneTexts` and land on `fields[zoneId]`, and both save paths in `App.jsx` convert every `*Url` blob. Verified with a node check of the mapping; the real chat, real save and reviewer page were not exercised end to end.
- **Rotated zone (T&Cs) guide drawn 90 degrees off in the editor.** `TemplateCanvas.jsx` rotated a rect that already had the visual (narrow x tall) width/height, so it lay sideways off the canvas edge, unlike the Import overlay. Width/height are now swapped for a +-90 zone. Verified in the local editor on Option A.
- **Import page: H slider on a rotated zone didn't update `textWidth`.** Option C's `tc` was saved with height 127 but textWidth 150.3. The H slider now keeps them in step and "Save zone settings" forces `textWidth = height` for every rotated zone, so Option C corrects itself on its next save.
- **Import page centre guides + snap** (Julia's ask): dashed amber centre lines on the overlay, X/Y sliders snap to the canvas centre within 3 canvas units (pointer drags only - typed numbers and arrow keys never snap), and "Centre horizontally / vertically" buttons per zone that show a tick once centred. The background art's margins are symmetric so canvas centre is also the teal card's centre.
- **"Move to folder" appeared to work but the design ended up back in its old folder.** Root cause: every save writes the full project into `sessionStorage` (`wildcast_project_<id>`) and re-opening reads that copy first; move and rename only patched the server, so re-opening loaded the old folder/owner/name and the next autosave wrote them back over the move. `DesignsPage.jsx` now patches that cached copy on move and rename, and a rejected move rolls back and alerts instead of being silently ignored. Verified in local dev with stubbed API calls; the real `/api/move-project` was not exercised.
- Checked and NOT a bug: for Option C the saved non-rotated zones matched exactly between the Import overlay and the editor (background has ~3.9% side / ~2.7% top margin baked in and both views honour it). Whether text sitting at the top of its box in the editor (rather than centred) accounts for the rest of what looked "a bit off" is unconfirmed.

### 2026-09-22 — Pre-filled placeholders, role toggle, and three Notion backlog cards, all shipped to production same day

All of today's work went through short-lived feature branches (`git checkout -b`, Vercel branch previews, Julia clicked through each before merge) rather than straight onto `main` - first time this session used that workflow end to end. All items below are merged and live on `cast.wildstack.studio`.

**33. Pre-filled Template Placeholders** (Notion card, Annika - "a template never opens empty"). Every text/image field now opens with greyed, generic placeholder content (headline/sub_headline/offer/restaurant_name are `omnes-cond`/WOLT Cond - always-caps display font, matched to how `App.jsx` already auto-uppercases real typed content in those zones; tc/cta are `omnes-pro` body text and stay normal case) instead of an empty box. Clicking a placeholder text field selects it so typing replaces immediately (`FieldEditor.jsx`); a small ↺ resets an edited field back to its placeholder. Placeholder content is purely a display-time substitution (`src/data/placeholders.js`'s `TEXT_PLACEHOLDERS`/`IMAGE_PLACEHOLDERS`/`placeholderTextFor`) - never written into `fields` state, and explicitly hidden before `TemplateCanvas.jsx`'s `getPng()` runs, so it can never end up baked into a real exported/saved/sent flyer. Headline/subline placeholders were later shortened to literally "HEADLINE"/"SUBLINE" per Julia's ask. Scoped down from the card's original ask after investigating: the Figma import pipeline (`api/_lib/figma-import.js`) only ever captured geometry/font/color, never the real designed text/images, so "the template's own original design as placeholder" isn't actually possible today - these are deliberately generic stand-ins instead, flagged to Julia before building.

Also fixed in passing: a latent race in `TemplateCanvas.jsx`'s image-sync effect (`fabric.Image.fromURL` callback checked `!fabricRef.current` instead of comparing against the specific canvas instance it was called for) that crashed the canvas (`Cannot read properties of null (reading 'clearRect')`) under React StrictMode's dev-only double-mount - previously latent since most image zones had nothing to load on first mount; placeholders made every zone always have *something* to load, surfacing it every time. Fixed by comparing identity, not truthiness. Commits `d02efcc`, `1969fe7`.

**34. Designer/Reviewer/Manager workflow-role toggle + Export PDF gated to Manager.** A dropdown in its own row above the main header nav (Julia's ask, after the first two attempts - a pill toggle, then a same-row dropdown - both still crowded/overlapped "Help" at various widths), open to anyone signed in, deliberately separate from `activation.role` (agency/designer/partner, which still gates Import unchanged). Export PDF now additionally requires `workflowRole === 'Manager'`, enforced both in `FieldEditor.jsx`'s button state and as a belt-and-suspenders guard in `App.jsx`'s `handleExport`. Purely a client-side/localStorage toggle for now - explicitly provisional, no real per-account roles yet. Commits `00fba81`, `3a21440`, `3707a23`.

**35. Header rebuilt as a real flex layout.** Root cause of the repeated overlap above: the nav was `position: absolute` + `translateX(-50%)`-centered regardless of how much room the logo/right-side group actually needed. Replaced with logo (fixed) / nav (`flex: 1`, centered within its own remaining space, wraps onto a second line rather than ever overlapping) / right-side group (also wraps) - verified down to 640px wide, including with the credits/sign-out group present (mocked locally, no backend to populate `activation.clientName` in local dev). Commit `765f1b5`.

**36. Seat cap removed for the pilot.** Julia's ask: "remove the seat limit, this is just during the pilot." `SEAT_CAP` (`api/_lib/accounts.js`) is `Infinity` instead of `5` - the one constant both the signup block (`api/account-auth.js`) and the seat-count display (`api/account-seats.js`) already key off, so restoring a real number later is a one-line change. `ActivationGate.jsx`'s "X of Y seats available" banner hides itself when uncapped (`Infinity` has no JSON representation and serializes to `null`, which is what its `Number.isFinite` check keys off) instead of showing "null of null." Commit `1b51e76`, merged as its own PR (#17) ahead of everything else that day since it was blocking real pilot signups.

**37. "Resolve and resubmit."** Notion card, Julia, self-assigned: "Mark comments as resolved and send the same asset back for review in one click." New button at the bottom of the editor's Feedback sidebar - marks every open comment resolved (parallel PATCHes, same request shape as the existing per-comment checkbox) and immediately re-saves the same project via `doSave()`, instead of checking each comment then separately clicking Send for Review. No new asset - `doSave()` already keys off `currentProjectId || crypto.randomUUID()`. Verified locally (mocked comment/save endpoints): both comments flip to resolved with strikethrough, review link byte-identical before/after. Commit `729521f`.

**38. "Partner review link" - name prefill for signed-in visitors.** Notion card: "A person with no account opens a link, types their name, and leaves comments. Account holders get their name prefilled." The no-account half already worked (`ReviewPage.jsx`'s review link was always reachable without activation - see `App.jsx`'s "unless already activated or this is a shared review link" gate). Added the prefill: `activation?.clientName` passed into `ReviewPage` as `reviewerName`, seeded into the name field's initial state plus a `useEffect` that fills it in if it arrives after mount (App's own `activation` state resolves asynchronously - `clientName` is empty on the very first paint every time) - guarded with `prev || reviewerName` so it never clobbers a name the visitor already typed. Not fully exercisable in local dev (the review screen only mounts via a hard page load reading `?review=<id>` from the URL, nothing to mock against before that first paint locally) - flagged to Julia for a live click-test; she confirmed working. Commit `84930b7`.

**39. "My Tasks" - a per-user review queue.** Notion card: "A simple task board under the profile. Each asset shows its state: under design, under review, approved. No more than that." New persisted `reviewStatus` field (`'design' | 'review' | 'approved'`) on the project record - didn't exist before (the closest thing, `reviewSent`, is explicitly session-local and resets every reopen). `doSave()` takes an optional `nextReviewStatus` override; Send for Review passes `'review'`, every ordinary Save/autosave omits it and preserves whatever status already is; starting a brand-new project resets it to `'design'`; opening a saved one restores it from the record. Only a new Approve button on `ReviewPage.jsx` (the reviewer's own page) ever sets `'approved'` - nothing on the creator's side can. New PATCH handler folded into `api/save-project.js` rather than its own route, matching this codebase's own precedent of merging small routes to stay under Vercel's per-deployment function-count limit (`comments.js`'s earlier merge of get-comments.js + add-comment.js). New `MyTasksPage.jsx`, nav item grouped with the other browse-your-stuff pages (Templates/Design library), not next to New Brief. Verified locally end to end except literally clicking Approve (same hard-navigation local-testing limit as item 38): My Tasks correctly grouped mocked designs 1/3/1 across the three columns (matching the card's own test line exactly), Send for Review correctly persisted `reviewStatus:'review'`, a plain re-save preserved it, and simulating a successful Approve PATCH correctly moved a design between columns. Julia confirmed the real Approve button working live. Commit `3d1bfe7`.

**40. Follow-up: "Resolve and resubmit" now also updates `reviewStatus`.** Items 37 and 39 were built on separate branches, neither aware the other existed - `handleResolveAndResubmit` didn't yet know about `nextReviewStatus` since that override didn't exist yet when it was built. Closed once both were merged: a resubmit is exactly the "back under review" transition My Tasks needs to reflect, same as Send for Review. Verified live: Send for Review then Resolve and resubmit both correctly save `reviewStatus:'review'` against the same project id. Commit `1a1cc78`.

All items verified live on `cast.wildstack.studio` post-merge except where noted above.

### 2026-09-23/24 — "Request changes" review outcome, and headline/placeholder accuracy fixes (built 2026-09-23, merged to `main` 2026-09-24 as PRs #27 and #29)

**41. "Request changes" as a distinct review outcome, plus five rounds of self-review bug-fixing on top (PR #27).** Mark's ask via Julia: a submitted design just sat at "under review" forever whether a reviewer approved it, said nothing, or wanted changes - no way for the creator to tell those apart. `ReviewPage.jsx` gets a "Request changes" button next to Approve, enabled only once there's at least one open comment (so a request always comes with a written reason); it sets a new `changes_requested` `reviewStatus`, and `MyTasksPage.jsx` gets a matching "Needs changes" column between "Under review" and "Approved" (later relabelled "Ready for review" / "Adjustment needed" per Julia's wording ask, same underlying status keys). "Resolve and resubmit" needed no changes to handle it - it already unconditionally flips `reviewStatus` back to `'review'` on resave.

The feature itself was a small change; getting it right took five rounds of Julia's click-testing plus code review, each surfacing real bugs that got fixed the same day: `everRequestedChanges` wasn't tracked in `App.jsx` state so every autosave silently dropped My Tasks' "second round" indicator; the whole left Review panel (holding Approve/Request changes) never mounted until a comment existed, making both buttons unreachable on a design nobody had commented on yet; a race condition around `reviewStatus`/`everRequestedChanges` living only in scattered client-side mirrors (never resynced from the server) could let a stale browser tab clobber a more recent change made elsewhere; a stale-closure bug meant `doSave()` sometimes ran with the previous render's `reviewStatus`, not the one just set; Approve/Request-changes from the editor patched the server but not the `sessionStorage` cache directly (only indirectly, via a call that could no-op); duplicating a design didn't reset `reviewStatus`, so a copy of an already-submitted design read as a resubmit on its first real send; and "Resolve and resubmit" stayed clickable (and left the user sitting on the canvas looking like nothing happened) even after there was nothing left to resubmit. All fixed same-session, each verified against Julia's specific click-testing report before moving to the next round. Final commit `7078e79`.

**42. Guided-only editor, real-flyer placeholder copy, and the auto-resize groundwork the fixes above needed (yesterday's placeholder/fontsize branch, commits merged into `main` via PR #29 together with item 43 below).** Several related changes, all from Julia's testing on 2026-09-23:
- **Designer mode and the "Choose your mode" popup are gone from the UI** - opening any template now goes straight into the Guided editor (canvas locked, side-panel controls only). Designer mode's code isn't deleted, just unwired, same precedent as the old AI-candidate-picker flow.
- **Auto-resize headlines/sublines to fill their bounding box**, not just shrink long text - short text now grows back up to the zone's designed size instead of sitting at whatever the last shrink left it at.
- **Real-flyer placeholder copy per catalogue option** (`src/data/placeholders.js`'s `TEMPLATE_TEXT_PLACEHOLDERS`) - replaces the generic "HEADLINE"/"SUBLINE" stand-ins with copy that mirrors each option's real artwork (this is what item 43 below then had to correct for Option A/C).
- **Placeholder guide text always renders static and centered** at the zone's designed position/size - a saved Designer-mode drag from before Designer mode was hidden can no longer shove placeholder text off-canvas, and imported template zones are normalized to always-`autoShrink`/centered at merge time (fixes older Figma imports retroactively, no re-import needed).
- **Long headlines losing centered alignment after auto-resize, fixed** - Fabric's `Textbox` silently widens itself past a fixed `width` whenever a single word doesn't fit at the current font size, and never narrows back down; since every zone is left-anchored, a silently-widened box stayed pinned at its left edge with its right edge drifting off-canvas. `applyFontSizeAndCheckFit()` now pins the box back to the zone's real width on every size attempt, folding "a word didn't fit" into the same overflow signal already used for height.

**43. Option A/C headline-subline accuracy pass, and per-template photo placeholders (today, 2026-09-24).** Julia flagged real discrepancies between the guided canvas and the actual campaign artwork (Wen Cheng, McDonald's, Akko), comparing screenshots side by side.
- **Option A/C: sub_headline was duplicating `restaurant_name`'s content.** Item 42's new real-flyer placeholder copy gave Option A's `sub_headline` zone "Wen Cheng ♥ Wolt" - but that's already what the separate `restaurant_name` zone renders (right-aligned, paired with a "♥ WOLT" wordmark baked into the background art, precisely positioned right under the headline). Since `sub_headline` sits in its own zone *above* the headline, this produced two "Wen Cheng ♥ Wolt"-ish lines sandwiching the headline instead of the real flyer's actual two-line hero text. Fixed by splitting the real flyer's headline properly across the two zones instead: Option A's `sub_headline`/`headline` now read "Potsdams neues" / "Dreamteam", Option C's read "Chick this out," / "Berlin." - matching each real flyer's own two-line treatment.
- **Per-template photo placeholders.** Every option's `photo` zone now shows a translucent preview of the *real* campaign photo (Wen Cheng dishes / McDonald's Big Mac / Akko chicken, WildScale-prepped transparent PNGs from Julia) instead of a flat grey "PHOTO" box, via a new `placeholderImageFor()` alongside the existing `placeholderTextFor()`.
- **T&C copy fit verified, not just eyeballed** - measured the generic T&C placeholder against Option A's and Option C's real font/box geometry with actual canvas text metrics (not guessed): both wrap to 2 lines with less than half their rotated sidebar's width used, comfortable margin either way.
- **Open item, not yet resolved:** mid-session, giving `sub_headline`/`headline` their own explicit per-zone fit overrides (to reach their full Figma-calibrated 56.6pt/38.78pt) made Option A's headline "far too big" per Julia - reverted back to the original shrink-to-fit behavior (`2b455c1`), which she'd already confirmed looked right. She separately flagged that the *original* behavior "did not auto-scale" (exact symptom unconfirmed - typing short/long text, clearing a field, and centering on a long unbreakable word all behaved correctly in direct testing this session). Flagged back to her for a specific repro before touching this zone's sizing again. Commits `64955b4`, `09e9492` (reverted), `2b455c1`.

Merged to `main` as PR #27 (`2a2f86e`) and PR #29 (`79dc9d1`), 2026-09-24. Production deploy verified via a real `/api/validate-key` check, not just a green build.

### 2026-09-24 (continued) — CMYK color-matching review, AI Suggest scoped down, My Tasks race condition fixed

**44. Real bug found and fixed: PDF/X OutputIntent subtype was misspelled since the export was first built.** A colleague's (Annika's) color-matching report on the Wolt x McDonald's flyer compared against InDesign's own export and flagged several possible causes; cross-checking each one against the real code (not just trusting the report) turned up a genuine, independent bug: `api/export-cmyk.js`'s OutputIntent dictionary wrote `/S /GTS_PDFIX` - the real PDF/X subtype identifier is `/GTS_PDFX` (no "I"), unversioned regardless of X-1/X-3/X-4. A strict print RIP checking this key for PDF/X-conformance could fail to recognize the file and ignore the embedded FOGRA51 output intent entirely. Fixed and verified by actually running a real PNG through the live handler end to end and confirming the generated PDF contains `/GTS_PDFX` with zero remaining occurrences of the typo - not just a code read. PR #30, commit `7058e89`.

**45. CMYK color-matching investigation (Annika's report) - most findings confirmed real, one "quick fix" isn't actually quick.** The report's core diagnosis - WildCast converts brand hex colors (RGB) through an ICC profile, while InDesign uses a brand's separately-defined, hand-picked official print CMYK (e.g. Wolt Blue: hex #00C2E8 vs print C75/M0/Y10/K0, a ΔE ~2.8 visible mismatch) - checked out on direct inspection of the code: there's no brand-color lookup anywhere (`placeholders.js`/`templateZones.js`/`export-cmyk.js` all confirmed clean), so this is architecturally inevitable today, not a bug. Also confirmed: the whole exported page really is one flat raster image (the Fabric.js canvas gets rasterized client-side into a single PNG before conversion) - no vector text or logos anywhere in the output, so small text prints in mixed CMYK ink instead of pure K. Also confirmed: the RGB→CMYK conversion runs in Perceptual rendering intent, not Relative Colorimetric + black point compensation - but this is **not a quick fix**, since `sharp` 0.33.5 (the only library in this pipeline that does the conversion) doesn't expose an `intent` parameter in its JS API at all (checked the installed library's actual source, not docs) - achieving real rendering-intent control would need a different library or a lower-level color-management tool, not a one-line change. No code shipped from this investigation besides item 44's typo fix - the real fix (a brand color library, i.e. Option A from Annika's writeup: a per-brand hex→CMYK lookup table used at export time) is a real, not-yet-started feature, not something partially built already.

**46. Ghostscript for CMYK conversion was never implemented here - confirmed by searching, not assumed.** Separately asked whether a Ghostscript-based backend (mentioned in `WILDCAST_CLAUDE.md`'s original Phase 2 spec, and separately the subject of a previous, failing Digital Ocean-hosted attempt for the *Wild CMYK* Figma plugin - a different, sibling tool) exists or works in this repo. It does not: no `gs` binary anywhere in the repo, no `child_process`/`execFile`/`spawn` calls anywhere in `api/*.js`. Traced the actual decision to the very first STATUS.md commit (2026-06-19): *"No Ghostscript, no Railway, no extra accounts"* - Ghostscript is a native binary that can't run in a normal Vercel serverless function without either an unproven binary-bundling spike (50MB function limit) or standing up a second hosting platform (Railway) alongside Vercel. `sharp` (a precompiled native library shipped as a normal npm package, no separate binary to manage) does the same real ICC-based conversion without either cost. **Worth remembering before Ghostscript gets re-proposed for this app specifically** - it wasn't rejected for being incapable, it was rejected to avoid a second piece of infrastructure, and that reasoning still applies.

**47. AI Suggest restricted to Headline and Sub-headline only (Notion card, Anang - "Remove AI Suggest on the fields where it's inappropriate").** Landed as two separate, sequential PRs after a real same-day collision: Anang independently built and merged his own fix (PR #32, `d0628f8`) covering Restaurant name, Offer, and T&Cs (a `NO_AI_FIELDS` set in `FieldEditor.jsx`) while a duplicate version was being built in parallel elsewhere - the duplicate was closed unmerged once this was discovered, avoiding two redundant mechanisms in the same file. Julia's actual ask was narrower than the card's literal three fields ("only keeping AI Suggest on subline and headline"), so a small follow-up (PR #33, `45921b1`) added the fourth field his version didn't cover - Option B's "App download line" (`cta`) - to the same `NO_AI_FIELDS` set. "Choose preset" is unaffected on every field. Verified live on both templates: Headline/Sub-headline still show both Choose preset and AI Suggest; Restaurant name/Offer/T&Cs/App download line show Choose preset only.

**48. Real bug found, root-caused, and fixed: a design started from scratch and sent for review could silently create two separate project records, so a reviewer's "Request changes" never reached My Tasks.** Julia's report: comment visible on the review link, card never appears in "Needs changes." Root cause, confirmed by reading the actual save code (not guessed): `doSave()` mints a project id with `currentProjectId || crypto.randomUUID()` whenever nothing's been saved yet. The 2.5s autosave debounce calls `doSave()` independently of any button click - if it fires within a couple seconds of a manual "Send for Review" click (an entirely normal pattern: finish typing, immediately click send), both calls can read `currentProjectId` as still `null` before either resolves its own `setCurrentProjectId(id)`, and each mints its own random id. Two records get created for one design; whichever id wins is what the editor keeps autosaving to, while the review link and the reviewer's PATCH operate on the other, now-orphaned record. Fixed with a ref-backed lazy id (`pendingNewIdRef` in `App.jsx`) instead of a bare `crypto.randomUUID()` call - a ref write is synchronous, so the first `doSave()` call to actually reach that line claims the id and every concurrent call reads the same one back, regardless of how many are in flight; reset alongside every existing "start a genuinely new project" reset point. Verified with a 500-trial simulation of the real timing pattern (old code: 421/500 trials produced two different ids; fixed code: 0/500) - a live click-through reproduction wasn't possible (the "Send for Review" confirmation is a native browser `confirm()` dialog that browser-automation tooling can't drive), so this rests on the code-level fix plus the simulation, not a watched end-to-end repro. PR #34, commit `83d853e`.

All items 44/47/48 verified live on `cast.wildstack.studio` post-merge (real `/api/validate-key` checks after each deploy, plus direct in-browser field checks for #47). Item 45/46 produced no shippable code beyond #44's typo fix - they're findings/decisions on record for whoever picks up the brand-color-library work next.
